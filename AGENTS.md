# Chemical Safety VR Server Repository Context

## 프로젝트 목적

이 저장소는 Tyche 화학 안전 교육 VR의 Express API 서버, 정적 웹사이트와 PPE 훈련 데이터 대시보드를 관리한다. Unity XR 클라이언트는 별도 비공개 저장소 `softCastella/chemical-safety-vr-client`에서 관리하며, 이 저장소에 Unity 프로젝트나 클라이언트 에셋의 복사본을 추가하지 않는다.

## 환경

- Node.js: 20 이상
- Express: 5.x
- 데이터베이스: MySQL (`mysql2`)
- 프로세스 관리: PM2
- 패키지 관리자: npm

## 주요 경로

- 애플리케이션 진입점: `src/server.js`, `src/app.js`
- 환경 설정: `src/config`
- DB 연결과 마이그레이션: `src/db`, `db/migrations`
- 사용자 기능: `src/modules/users`
- 훈련 등록: `src/modules/training-registration`
- 로컬 텔레메트리: `src/modules/local-telemetry`
- 정적 사이트와 대시보드: `public`
- 자동 테스트: `test`, `test-support`

## 작업 규칙

- `.env`, 운영 자격 증명, 개인키, 실제 사용자 데이터와 로컬 텔레메트리 원본을 커밋하지 않는다.
- 환경 변수의 이름과 예시는 `.env.example`에만 기록하고 실제 값은 저장소 밖에서 관리한다.
- `node_modules`와 테스트·런타임 생성 데이터는 버전 관리하지 않는다.
- 요청 범위와 무관한 API, fallback, 자동 수리, 데이터 분류 또는 운영 동작을 추가하지 않는다.
- 필수 설정이 없을 때 임의 기본값으로 운영 동작을 계속하지 말고, 설정명과 실패 지점을 명확히 보고한다.
- DB 스키마 변경은 새 마이그레이션으로 추가하고 이미 적용된 마이그레이션 파일을 소급 수정하지 않는다.
- 명시적인 요청 없이 운영 DB 마이그레이션, PM2 재시작, 배포 또는 운영 데이터 변경을 실행하지 않는다.
- 서버 코드 변경 후 `npm test`를 실행하고, DB나 배포가 필요한 검증은 정적 확인·로컬 확인·운영 확인으로 구분한다.

## Codex 행동 하네스 (필수)

이 절에서 `하네스`는 판단과 행동을 제한하는 규칙이고, `하네싱`은 아래 규칙을 현재 응답과 작업에 실제로 적용하는 행동을 뜻한다. 사용자가 `하네싱`이라고 지시하면 새 코드 하네스나 테스트를 만들지 말고 아래 절차를 즉시 적용한다.

1. **요청 유형을 먼저 고정한다.** 사용자 발화를 `질문·설명 요청`, `비판·정정`, `상태 확인`, `조사·진단`, `수정`, `커밋·푸시`, `배포·운영 변경`으로 구분한다. 질문, 비판, 정정과 원인 문의를 수정 지시로 확대하지 않는다.
2. **명시된 동사만 실행 권한으로 본다.** `고쳐`, `만들어`, `반영해`, `커밋해`, `푸시해`, `배포해`, `재시작해`, `마이그레이션해`, `삭제해`처럼 사용자가 분명히 지시한 범위만 수행한다. `왜`, `어떻게`, `보고 싶다`, `확인해 봐`를 코드 수정·배포 권한으로 해석하지 않는다.
3. **의미가 둘 이상이면 행동 전에 묻는다.** 서로 다른 해석이 파일 수정, 운영 변경 또는 결과 차이로 이어지면 한 문장으로 확인하고 답을 받을 때까지 변경 작업을 시작하지 않는다. 익숙한 프로젝트 용어라는 이유로 사용자의 일반 언어를 코드 용어로 좁혀 해석하지 않는다.
4. **사용자 정정 시 즉시 멈춘다.** 사용자가 의도나 범위를 정정하면 진행 중인 수정·배포·추가 조사를 멈추고, 정정된 사실과 현재 변경 여부만 보고한다. 사용자가 다시 요청하기 전에는 진단이나 구현을 이어가지 않는다.
5. **근거 단계를 분리한다.** `문서에 적힘`, `소스에 옵션이 존재함`, `호출부에 연결됨`, `빌드 산출물에 포함됨`, `운영에 배포됨`, `실제 화면·로그·DB에서 확인됨`을 별도 상태로 취급한다. 앞 단계만 확인하고 뒤 단계까지 맞다고 말하지 않는다.
6. **실제 관찰을 코드 추정보다 우선한다.** 사용자가 실제 화면이나 동작의 차이를 보고하면 소스 의도로 반박하지 않는다. 재현·렌더·로그로 확인하기 전에는 원인을 `미확인`으로 표시한다.
7. **사실·추론·제안을 섞지 않는다.** 확인한 사실에는 파일, 실행 결과 또는 운영 조회 근거를 붙인다. 추론은 `추정`, 아직 확인하지 못한 내용은 `미검증`, 앞으로 할 수 있는 일은 `제안`으로 구분한다.
8. **변경 전에 범위를 알린다.** 파일 수정이나 운영 변경이 명시적으로 요청된 경우에만 대상, 영향과 검증 방법을 짧게 알리고 수행한다. 사용자의 승인 범위를 넓히는 후속 변경은 다시 확인한다.
9. **완료를 과장하지 않는다.** 정적 테스트 통과를 실제 UI 일치나 운영 수집 성공으로 보고하지 않는다. 사용자가 요구한 최종 관찰 단계까지 확인하지 못했으면 완료가 아니라 남은 검증으로 보고한다.
10. **응답 전 자가 점검한다.** `요청받지 않은 변경을 했는가`, `확인하지 않은 것을 사실로 말했는가`, `사용자 관찰을 코드로 반박했는가`, `질문해야 할 불확실성을 추측으로 채웠는가`를 확인하고 하나라도 해당하면 실행하거나 확정 답변하지 않는다.

## Git 커밋·푸시 규칙

- 커밋 제목에는 한글을 포함한다.
- 커밋 본문에는 `1. `부터 시작하는 번호 목록으로 변경 내용과 검증 결과를 기록한다.
- 저장소의 `.githooks/commit-msg`, `.githooks/pre-push`와 `Tools/KoreanCommitMessageHarness.mjs`를 기준 하네스로 사용한다.
- 새 clone에서는 `npm run hooks:install`로 Git 훅을 활성화한다.
- 커밋·푸시 결과를 사용자에게 보고할 때도 한글 번호 목록으로 커밋 SHA, 제목과 푸시 대상을 구분한다.

## 클라이언트 연동과 교차 저장소 인수인계

- 서버와 클라이언트의 Codex 대화는 자동으로 공유된다고 가정하지 않는다. 코드, 문서, 테스트 결과와 Git 커밋을 작업 사실의 기준으로 사용한다.
- API, 데이터 계약, 인증, 텔레메트리 또는 대시보드 변경이 클라이언트에 영향을 주면 `softCastella/chemical-safety-vr-client`의 관련 코드와 문서를 확인하고 대상 브랜치·커밋 SHA를 작업 결과에 기록한다.
- 클라이언트 반영이 필요한 변경은 서버만 수정한 상태를 통합 완료로 보고하지 않는다. `서버 반영`, `클라이언트 반영`, `통합 검증` 상태를 구분한다.
- 최종 문서나 보고자료 작성 전 서버와 클라이언트 저장소의 기준 커밋을 각각 확인한다. 대화 요약만으로 다른 저장소의 구현 또는 검증 상태를 확정하지 않는다.
- 교차 저장소 계약 변경은 관련 기존 문서가 있으면 그 문서를 갱신한다. 관련 문서가 없을 때만 새 문서를 만든다.
- `Docs/SharedDocumentManifest.md`에 지정된 공용 문서는 클라이언트 저장소의 같은 상대 경로 기준본을 서버에 미러링한다. 서버 구현으로 공용 사실이 바뀌면 서버 미러를 갱신하고 `클라이언트 문서 동기화 필요` 여부와 서버 커밋 SHA를 작업 결과에 기록한다.

## 훈련 데이터와 보고자료 사실성 규칙

- 화면 명칭이나 사용자 설명만으로 기능 의미를 추론하지 않는다. 서버 핸들러, 데이터 저장소, 원본 이벤트와 필요한 경우 클라이언트 씬·런타임 코드를 함께 확인한다.
- `코드에 존재함`, `클라이언트 씬에 연결됨`, `실행 로그로 수집됨`, `서버·대시보드에서 조회됨`을 서로 다른 검증 단계로 구분한다.
- 모든 지표·원인·상태에는 근거 이벤트 또는 파일, 계산 규칙과 상세 조회 경로가 있어야 한다. 하나라도 없으면 확정 지표로 표시하지 않는다.
- 사용자에게 없던 UI, ID, 단계, 종료 원인 또는 분류를 확정 사실로 새로 만들지 않는다. 제안은 현재 사실과 분리한다.
- `EXIT Point 중도 중단`, 기존 종료 버튼, `application_quitting`, 미종료 세션을 구분한다. `application_quitting`만으로 전원 종료·충돌·실수 종료를 확정하지 않는다.
- 정적 계약 하네스 PASS를 실제 이벤트 수집 성공으로 확대 해석하지 않는다. 실제 수집 여부는 최신 원본 이벤트와 서버 조회 결과를 별도로 확인한다.
- 확인하지 못한 문장은 삭제하거나 `미검증`으로 표시한다.

## 문서 작성 원칙

- 사용자가 영문 작성을 명시하지 않으면 프로젝트 문서는 한국어로 작성한다.
- 새 문서를 만들기 전에 저장소에서 동일 기능·오류·계약을 다루는 기존 문서를 검색한다. 관련 문서가 있으면 새 문서를 만들지 않고 기존 문서를 갱신한다.
- 날짜가 붙는 기록은 회의록 또는 재현 가능한 버그 리포트에만 사용한다. `Daily`, `Worklog`, `데일리로그`, `작업일지` 형식의 새 문서를 만들지 않는다.
- 문서에는 적용 변경, 근본 원인, 영향 범위, 완료 검증과 남은 수동 검증을 구분한다.

## 세션 시작과 검증

- 작업 시작 시 이 파일을 기준으로 요청과 직접 관련된 서버 파일만 우선 조사한다.
- 업데이트나 창 종료 후 이전 Codex 세션을 복구할 때는 답변이나 수동 탐색보다 먼저 `npm run codex:session:find`를 실행해 현재 저장소와 작업 경로가 같은 세션을 확인한다. 이 하네스가 실패하기 전에 세션이 없거나 찾을 수 없다고 보고하지 않는다. 현재 터미널 재개는 `npm run codex:session:resume`, Windows 새 창 재개는 `npm run codex:session:open`을 사용하며 개인 세션 ID나 대화 원문을 저장소에 기록하지 않는다.
- 기본 회귀 검증은 `npm test`이다.
- DB 변경은 마이그레이션 SQL의 정적 검토와 별도 테스트 DB 검증을 구분한다.
- 대시보드·CSV·보고서 결과는 원본 이벤트, 계산 규칙과 조회 경로를 대조한 뒤 전달한다.
