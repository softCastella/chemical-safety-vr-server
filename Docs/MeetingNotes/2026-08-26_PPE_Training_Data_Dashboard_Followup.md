# PPE 훈련 데이터·대시보드 후속 회의록

- 일자: 2026-08-26
- 범위: Unity 훈련 계측, 로컬 Express 서버, JSONL 저장, 실데이터 대시보드
- 상세 데이터 계약 기준: [PPE 훈련 대시보드 개념 및 측정 기준](./2026-08-21_PPE_Training_Dashboard_Concept.md)

## 1. 회의 목적

오늘까지 연결한 Unity 훈련 데이터와 대시보드의 실제 검증 결과를 정리하고, 담당 선생님의 피드백인 “현재 화면은 DB 값을 그대로 보여 준 느낌이며, 이 데이터를 어디에 사용할 수 있는지가 표현되어야 한다”를 후속 설계 기준으로 확정한다.

이번 후속조치의 핵심은 데이터 항목을 더 많이 나열하는 것이 아니다. 각 데이터가 어떤 문제를 발견하고, 관리자가 무엇을 판단하며, 콘텐츠·상호작용·운영을 어떻게 개선하는지 연결하는 것이다.

## 2. 오늘까지 완료한 작업

### Unity 실데이터 계측

- 한 플레이 회차를 `sessionId`로 묶어 JSONL에 저장한다.
- Meta 앱 범위 사용자 ID와 연령 범주 상태를 세션에 연결한다.
- 교육·훈련·테스트 모드와 시나리오, 과정 단계 변화를 기록한다.
- PPE 검사 시작·종료, 장비 종류, 청결·오염 상태, 선택 승인·거부를 기록한다.
- 시나리오별 필수 PPE와 미착용 목록 변화를 기록한다.
- 음성 클립 길이, 실제 재생시간, 완료·교체·중단 상태를 기록한다.
- PPE 구역의 실제 Controller Grip 입력을 음성 Trigger와 분리해 기록한다.
- Grip 시도와 결과를 `attemptId`로 연결하고 손, 입력 경로, hover PPE, 처리시간을 저장한다.

### 로컬 서버 연결

- Unity가 `POST /api/training-registrations`로 세션과 Meta 사용자 정보를 전송한다.
- 서버가 JSONL에 저장한 값을 `GET /api/training-registrations/:sessionId`로 다시 반환한다.
- Unity는 POST 성공만으로 완료 처리하지 않고, 다시 읽은 `sessionId`와 Meta 사용자 ID가 일치할 때 `가입이 완료되었습니다.`를 출력한다.
- 실제 세션 2건과 교사 왕복 시험 1건의 `serverReceivedAt`이 로컬 서버 기록에 남아 있다.

### 실데이터 대시보드

- 기존 모의 사용자·모의 세션·근거 없는 경고 수치를 제거했다.
- `Meta 사용자 ID → sessionId → 모드·시나리오 → 단계 → PPE·음성·Grab → 원본 이벤트` 관계로 조회한다.
- 사용자별, 시나리오별, PPE별, 단계별 데이터를 같은 세션 원본으로 연결한다.
- 검토 신호와 표의 행을 누르면 관련 원본 이벤트로 이동한다.
- JSON 파싱 오류가 발생하면 파일명, 행 번호, 파서 메시지, 깨진 원본 행을 표시한다.
- 폴더의 HTML을 `file://`로 직접 열어 API 호출이 실패하는 문제를 확인했다. 앞으로는 자동으로 `http://127.0.0.1:3000/dashboard/`로 이동한다.

### 실제 검증 결과

- 실제 세션: 8건
- 유효 이벤트: 828건
- JSON 파싱 오류: 0건
- 식별된 Meta 사용자: 1명
- 미식별 세션: 2건
- `flowState=Completed` 관찰 세션: 1건
- 완료 전 종료 세션: 7건. 종료 이유는 아직 확정하지 않음
- 최신 OpenXR 세션의 Grip 시도: 4회
  - `selected`: 2회
  - `no_ppe_hover`: 2회
- 관련 서버 자동 테스트 12건과 대시보드 JavaScript 구문 검사를 통과했다.

## 3. 담당 선생님 피드백

### 피드백 요지

현재 대시보드는 DB 또는 원본 로그에 저장된 항목을 화면에 그대로 출력한 인상이 강하다. 데이터가 존재한다는 사실만 보여 주지 말고, 이 데이터를 교육 관리·콘텐츠 개선·사용성 점검·운영 검증 중 어디에 활용할 수 있는지를 화면에서 이해할 수 있어야 한다.

### 피드백에 따른 판단

원본 데이터 표는 증거 확인을 위해 필요하지만 대시보드의 첫 화면이 되어서는 안 된다. 첫 화면은 다음 질문에 답해야 한다.

1. 지금 무엇을 먼저 확인해야 하는가?
2. 어느 사용자·모드·시나리오·단계·PPE에서 문제가 발생했는가?
3. 그 판단의 근거가 되는 원본 이벤트는 무엇인가?
4. 확인 결과 어떤 수정이나 운영 조치를 할 수 있는가?

대시보드는 `데이터 → 의미 → 조치 → 원본 근거` 순서로 읽혀야 한다.

## 4. 데이터 활용 관계

| 데이터 원본 | 알 수 있는 내용 | 활용 목적 | 가능한 후속조치 |
|---|---|---|---|
| 단계 전환 시각 | 특정 단계의 체류시간과 마지막 도달 단계 | 과정 병목 확인 | 안내 순서, UI 위치, 단계 난이도 수정 |
| Grip 시도·결과·hover PPE | 잡기 성공, 빈 공간 Grip, hover 후 Select 실패 | PPE 상호작용 사용성 점검 | Marker, Collider, Interaction Layer, Grab 범위 점검 |
| PPE 검사시간 | 장비별 확인에 걸린 시간 | 식별하기 어려운 PPE 확인 | 모델 대비, 결함 표현, 패널 설명 개선 |
| 청결·오염과 선택 결과 | 오염 장비 거부 및 정상 장비 선택 여부 | 안전 판단 이해도 확인 | 오염 시각물, 피드백 음성, 교육 문구 개선 |
| 필수 PPE 누락 목록 | 시나리오별 미착용 장비와 진행 변화 | 필수 장비 이해 및 착용 진행 확인 | 필수 목록 안내, 체크리스트, 착용 순서 개선 |
| 음성 길이·실제 재생시간 | 끝까지 들은 음성과 중간에 교체·중단된 음성 | 음성 안내 부담 확인 | 긴 음성 분할, 반복·순서·재생 조건 수정 |
| 사용자 ID·세션·모드·시나리오 | 같은 사용자의 반복 수행과 모드별 변화 | 교육 효과와 재수행 변화 확인 | 개인별 보충 교육, 모드 구성 비교 |
| 과정 완료·종료 직전 상태 | 완료 여부와 종료 직전 단계 | 이탈 구간 확인 | EXIT Point·종료 버튼 구분 계측 후 단계별 이탈 개선 |
| 서버 수신 시각 | Unity 전송과 서버 저장의 연결 여부 | 수집 파이프라인 운영 점검 | 전송 실패, 재시도, 네트워크·Endpoint 점검 |
| JSON 파싱 오류 원문 | 저장 형식이 깨진 파일과 행 | 데이터 무결성 점검 | 직렬화 문자 정제, 손상 레코드 수정 |

## 5. 대시보드 후속 설계 원칙

### 첫 화면

- 단순 이벤트 건수보다 `확인 필요 항목`을 먼저 보여 준다.
- 각 항목에 문제의 의미와 관리 목적을 한 줄로 설명한다.
- 경고 수치를 누르면 해당 사용자·세션·단계·PPE와 원본 이벤트로 이동한다.
- 정상 기록도 함께 표시해 문제 기록과 비교할 수 있게 한다.

### 상세 화면

- 사용자 상세에서는 모드, 시나리오, 단계, PPE, 완료시간, 반복 수행 변화를 한 흐름으로 보여 준다.
- 시나리오 상세에서는 필수 PPE 전체 목록과 실제 착용·누락·오염 선택 결과를 연결한다.
- PPE 상세에서는 검사시간, Grip 시도, 성공·실패, 선택 결과, 실제 착용 완료를 구분한다.
- 사용성 화면에서는 문제 수치뿐 아니라 해당 문제를 어떤 UI·Collider·음성·상호작용 수정에 활용할 수 있는지 표시한다.

### 지표 표시 조건

모든 지표에는 다음 네 요소가 있어야 한다.

1. 지표 정의와 계산식
2. 사용한 원본 이벤트
3. 계측된 세션 범위와 미계측 세션 구분
4. 해당 값으로 판단하거나 수정할 수 있는 대상

원본 이벤트가 없거나 경로가 구분되지 않은 값은 `0`이나 추정 원인으로 표시하지 않고 `미계측` 또는 `원인 미확인`으로 표시한다.

## 6. 후속조치

### 1순위: 현재 데이터로 바로 표현 가능한 활용

- 단계별 병목과 마지막 도달 단계에 `과정 개선용` 설명 추가
- PPE별 검사시간·선택 결과·Grab 결과에 `상호작용 및 장비 표현 개선용` 설명 추가
- 음성 완료·교체·중단에 `음성 길이·순서 개선용` 설명 추가
- JSON 파싱 오류와 서버 수신 여부에 `데이터 수집 안정성 점검용` 설명 추가
- 각 활용 카드에서 관련 세션과 원본 이벤트로 이동

### 2순위: 이미 저장된 데이터의 추가 연결

- Unity 세션 JSONL과 서버 등록 JSONL을 `sessionId`로 결합해 서버 수신 여부 표시
- `scene_loaded`를 이용해 앱 시작부터 PPE 룸 진입까지의 구간별 시간 표시
- `required_ppe_status_changed`를 이용해 필수 PPE 누락 개수의 변화 표시
- Meta 진단 상태와 미식별 세션을 연결해 계정 수집 성공 여부 표시

### 3순위: Unity 추가 계측 후 표현

- 선택 승인과 실제 착용 완료를 구분하는 `ppe_equipped`
- 전체 14종 PPE와 시나리오별 필수 목록의 세션 시작 스냅숏
- EXIT Point, 종료 버튼, 앱 생명주기를 구분하는 종료 경로 이벤트
- 태블릿 시작·완료, 퀴즈 문항별 선택·정답·최종 점수
- 텔레포트 시작·취소·도착과 Trigger 음성 스킵
- PPE hover 시작·종료 및 `hover → Grip → Select` 구간 시간
- 앱 버전, Android version code, 실행 플랫폼, 장치·XR runtime

추가 계측은 실제 Unity Play/Quest JSONL 검증 전까지 대시보드에 측정 완료로 표시하지 않는다.

## 7. 근본 원인과 영향 범위

### 근본 원인

초기 대시보드는 어떤 값을 저장할 수 있는지 확인하는 데 집중해, 값이 관리자의 판단과 개선 행동으로 어떻게 이어지는지 설명하지 못했다. 원본 데이터와 요약 수치의 연결은 확보했지만, 요약 수치가 필요한 이유와 실제 활용 맥락이 화면에 부족했다.

### 영향 범위

- 대시보드 정보구조와 설명 문구
- 집계 API의 세션·서버 수신 데이터 결합
- Unity의 추가 텔레메트리 이벤트 계약
- 운영자용 문제 확인 및 원본 증거 이동 흐름

Unity의 기존 교육 흐름, PPE Grab, UI, 음성 재생 동작은 이번 회의록 작성으로 변경하지 않는다.

## 8. 완료한 검증과 남은 검증

### 완료한 검증

- Unity JSONL 8세션·828이벤트·파싱 오류 0건 확인
- OpenXR Controller Grip 시도와 결과의 `attemptId` 연결 확인
- Unity → 로컬 서버 POST → JSONL 저장 → GET 재조회 확인
- 실데이터 API와 대시보드 조회 확인
- 사용자·시나리오·PPE·단계·원본 이벤트 연결 확인
- 폴더 직접 열기 실패 원인 확인 및 서버 URL 자동 이동 코드·테스트 추가

### 남은 검증

- 선생님 피드백을 반영한 `데이터 활용 목적` 중심 화면 재구성
- 서버 수신 정보와 Unity 세션의 대시보드 결합
- 추가 이벤트 계측 후 Unity Play·Quest 독립 실행 검증
- 운영 DB·인증·HTTPS 적용 뒤 Meta Horizon 배포 환경에서 원격 수집 검증

## 9. 최종 결정

대시보드는 DB 뷰어가 아니라 교육과 시스템을 개선하기 위한 관리 도구로 정의한다. 원본 데이터는 결론이 아니라 근거이며, 화면은 `어디에 문제가 있는가`, `왜 확인해야 하는가`, `무엇을 수정할 수 있는가`, `원본 근거는 무엇인가`를 한 흐름으로 제공해야 한다.

## 10. 선생님용 설명 언어 개편 적용

### 적용한 변경

- 첫 화면 제목을 `실제 데이터 대시보드`에서 `VR 안전교육 수업 점검판`으로 바꾸고, 처음 보는 사용자가 `수업 완료`, `필수 장비`, `조작·안내` 순서로 읽도록 안내 영역을 추가했다.
- 메뉴를 `전체 개요`, `세션 상세`, `시나리오·PPE`, `사용성·그랩`, `원본 이벤트`에서 `수업 현황`, `한 회차 살펴보기`, `안전장비 학습`, `조작·안내 점검`, `기술 근거`로 바꿨다.
- `Education`, `ConfinedSpace`, `PpeArea`, PPE enum, 이벤트 코드와 조작 결과를 교사가 이해할 수 있는 한국어로 표시한다. 기술 코드명은 근거 확인이 필요한 화면에 보조 정보로 유지한다.
- 첫 화면의 원본 이벤트 건수와 데이터 품질 정보는 `기술 기록 상태 보기 (담당자용)` 안으로 이동하고, 과정 완료와 마지막 도달 단계 등 수업 판단 정보를 먼저 표시한다.
- 각 상세 영역에 `어떻게 활용하나요?` 설명을 추가해 단계 체류시간, 장비 판단, 잡기 조작, 음성 종료 상태가 어떤 교육·콘텐츠 개선으로 이어지는지 제시한다.
- 기존 `Grab 실패` 집계에는 `release_requested`, `released` 같은 정상적인 놓기 동작도 포함될 수 있어 표현을 수정했다. `no_ppe_hover`만 `장비를 가리키지 않고 잡기 시도`라는 도움 필요 신호로 구분하고, 놓기 동작은 실패로 세지 않는다.

### 근본 원인

기존 화면은 데이터 계약과 원본 근거를 정확히 연결하는 데 집중해 내부 상태명과 이벤트명이 화면의 주어가 됐다. 이 때문에 처음 보는 교사는 숫자가 수업의 어떤 문제를 뜻하고 무엇을 확인해야 하는지 해석하기 어려웠다. 또한 서로 다른 의미의 조작 결과를 성공 이외의 값이라는 이유만으로 실패처럼 표현한 부분이 있었다.

### 영향 범위

- 변경 파일: `server/public/dashboard/index.html`, `server/test/health.test.js`
- 변경 범위: 대시보드 정보 구조, 한국어 설명 문구, 코드값 표시명, 장비 잡기 결과의 화면상 분류
- 보존 범위: Express API, JSONL 원본, 필터 상태, 세션·학생·장비·근거 이동, Unity 교육·PPE Grab·음성·XR 동작
- 새 지표나 종료 원인은 만들지 않았다. 기록되지 않은 값은 계속 `아직 기록 없음`, `학생 확인 안 됨`, `이유는 아직 알 수 없음`으로 표시한다.

### 완료한 검증

- 대시보드 JavaScript 구문 검사 통과
- 서버 자동 테스트 13건 통과
- `git diff --check` 통과
- 로컬 서버의 대시보드와 텔레메트리 API HTTP 200 확인
- 검증 시점 실데이터 8회 수업·284개 이벤트·읽기 오류 0건 확인
- 선생님용 질문 안내와 `밀폐공간 작업`, `보호구 학습 구역`, `장비를 가리키지 않고 잡기 시도` 표시가 실제 제공 HTML에 포함됨을 확인
- `PPETrainingDataContractHarness.Validate()` PASS

### 아직 필요한 수동 검증

- 연결 가능한 브라우저 인스턴스가 없어 실제 렌더링 화면 캡처, 메뉴 클릭, 데스크톱·모바일 가로 잘림 검증은 수행하지 못했다.
- 담당 선생님이 처음 화면을 보고 별도 설명 없이 `완료 전에 끝난 수업`, `필수 장비`, `도움이 필요해 보인 시도`의 의미와 후속조치를 이해하는지 사용자 확인이 필요하다.

## 11. 2026-08-27 기업 직원 교육관리·평가·서비스 개선 방향 정정

### 사용자 피드백과 범위 정정

이 대시보드는 학교 수업용 안내판이 아니라 기업 단체 직원의 VR 안전교육을 관리하고, 직원 평가 근거와 서비스 개선 분석을 제공하는 운영 제품이자 포트폴리오다. 이전의 `처음 보신다면`, 질문형 안내와 `학생`, `수업` 중심 표현은 쉬운 설명을 과도하게 적용해 제품의 대상과 전문성을 잘못 전달했다.

### 적용한 변경

- 제품명을 `TYCHE VR 안전교육 운영 대시보드`로 바꾸고 `Enterprise Safety Training Analytics` 톤으로 정리했다.
- 메뉴를 `운영 개요`, `세션 분석`, `직원별 이력`, `PPE 수행 분석`, `UX·콘텐츠 품질`, `감사 로그`로 재구성했다.
- 질문형 초보자 안내를 제거하고, 상단에 실제 데이터 활용 시점을 다음 네 범위로 표시했다.
  - 세션 종료 직후: 과정 완료, 마지막 단계, 필수 PPE 상태를 이용한 교육 운영
  - 주간·월간: 동일 앱 사용자 ID의 반복 교육 이력을 이용한 보충교육 후보와 취약 PPE 선별
  - 릴리스 전후: 단계 체류, PPE 조작, 음성 중단의 반복 신호를 이용한 서비스 개선
  - 추가 연동 후: 사번·소속, Test 결과, 점수·합격 규칙을 이용한 공식 직원 평가·수료 판정
- 각 상세 카드의 설명을 `활용 시점` 중심으로 변경해 데이터가 언제 어떤 의사결정에 사용되는지 표시했다.
- `학생`, `수업`, `쉬운 질문` 표현을 `교육 대상자`, `교육 세션`, `운영·평가·품질 지표`로 교체했다.
- 감사 로그는 비즈니스 표시명과 원본 이벤트 코드를 함께 제공해 운영 지표와 평가 검토의 근거를 추적하도록 유지했다.

### 현재 데이터로 가능한 활용

| 활용 시점 | 현재 가능한 판단 | 근거 데이터 |
|---|---|---|
| 세션 종료 직후 | 과정 완료 여부, 완료 미확인 세션, 최종 단계, 필수 PPE 완료 상태 | `flow_state_changed`, `session_ended`, `required_ppe_status_changed` |
| 반복 교육 누적 후 | 같은 앱 사용자의 재교육 이력, 취약 PPE 후보, 보충교육 후보 | Meta 앱 범위 사용자 ID, `sessionId`, PPE 검사·판정 이벤트 |
| 서비스 개선 리뷰 | 반복되는 PPE 비대상 입력, 검사 지연, 음성 교체·중단 | `ppe_grab_*`, PPE 검사 시간, `voice_playback_ended` |
| 데이터 운영 점검 | 이벤트 수집 상태와 JSONL 손상 여부 | 원본 이벤트, 파싱 오류 파일·행 번호 |

### 공식 직원 평가에 추가로 필요한 계약

- Meta 앱 범위 ID와 기업 사번·성명·부서·교육 기수를 연결하는 권한 통제된 직원 디렉터리
- Education·Training과 분리된 Test 모드 실행 결과
- 태스크·퀴즈별 정답, 점수, 합격 기준과 재응시 규칙
- 평가 기간, 최신·최고·최초 시도 중 어떤 결과를 사용하는지에 대한 조직 정책
- 미수집·중도 종료·기술 오류를 실패 점수로 변환하지 않는 예외 규칙

위 계약이 연결되기 전에는 현재 Education 세션의 완료·조작 데이터만으로 공식 인사평가나 수료 판정을 확정하지 않는다.

### 근본 원인과 영향 범위

근본 원인은 `처음 보는 사람도 이해할 수 있는 설명`을 사용자 역할과 제품 맥락 없이 학교형 온보딩 문구로 해석한 것이다. 기업 교육관리자에게 필요한 것은 쉬운 질문이 아니라 활용 시점, 의사결정 범위, 평가 사용 조건과 원본 근거의 명확한 분리다.

변경 범위는 대시보드 정보 구조·문구·기업용 활용 기준, 관련 서버 테스트와 대시보드 컨셉 문서다. Express API, JSONL 원본, 집계 계산, Unity 교육·PPE·음성·XR 동작은 변경하지 않았다.

### 완료한 검증

- 대시보드 JavaScript 구문 검사 통과
- 서버 자동 테스트 13건 통과
- 로컬 서버 health·대시보드 HTTP 200 확인
- 기업용 제목, 데이터 활용 기준, 평가 제한 문구와 학교형 안내 제거를 실제 제공 HTML에서 확인
- `PPETrainingDataContractHarness.Validate()` PASS

### 아직 필요한 수동 검증

- 연결 가능한 브라우저 인스턴스가 없어 실제 렌더링 화면의 정보 밀도, 카드 높이, 데스크톱·모바일 가로 잘림과 메뉴 클릭을 확인하지 못했다.
- 포트폴리오 검토자와 기업 교육관리자가 상단의 네 활용 시점과 공식 평가 전제조건을 의도대로 이해하는지 사용자 검토가 필요하다.

## 2026-08-27 기업 운영·서비스 개선 정보구조 확장

### 이번 변경이 대응하는 사용자 요청

- 서비스 개선: 못 찾는 문제, 단계 전이 지연, 입력 지연, 설계 문제 후보와 업데이트 전후 변화
- 회원사별 직원 조회와 직원별 학습·수료 현황
- 직원 상세: 사용자 ID, Meta 계정 관련 값, 연령대, 시나리오·모드·PPE별 수행
- PPE 상세: 잡기 조작 결과시간, 선택 외 컨트롤러 입력, 수행시간, 시도·재시도, Test 결과
- 콘텐츠 품질: 시나리오·PPE별 개선 근거
- 이탈 분석: 서비스 문제와 개인 사정의 원인 구분

보존해야 하는 기존 동작은 JSONL 원본·Express API·Unity 교육 흐름·PPE 입력·음성 재생과 현재 계산 규칙이다. 이번 작업은 이 원본을 변경하지 않고 대시보드의 정보구조와 설명 경계를 확장했다.

### 적용한 변경

- 메뉴를 `운영 개요`, `서비스 개선`, `회원사·직원`, `직원 상세`, `콘텐츠 품질`, `이탈 분석`, `감사 근거`로 재구성했다.
- 서비스 개선 화면에 단계 체류 중앙값·최대값, Grip 입력부터 Select 결과까지의 `attemptElapsedSec`, 선택 외 잡기 결과, 음성 교체·중단을 표시했다.
- `단계 체류시간 = 못 찾음`, `attemptElapsedSec = 컨트롤러 하드웨어 입력 지연`, `단일 사용자 이상 = 잘못된 설계`로 단정하지 않도록 관측 신호·판단 한계·추가 계측을 같은 표에 표시했다.
- 업데이트 전후 화면은 `appVersion`, `contentVersion`, `releaseVersion`, `buildVersion` 중 실제 값이 있을 때만 버전별 집계를 표시한다. 기존 원본에는 해당 필드가 없으므로 `현재 전후 비교 불가`를 표시하며, 새 세션의 `session_started`부터 `Application.version`을 `appVersion`으로 기록한다. 버전이 하나뿐이면 기준선만 표시하고 전후 변화로 판정하지 않는다.
- 회원사·직원 화면에서 회원사 디렉터리, 사번·성명·부서, 공식 수료 규칙, Test 평가 계약의 연결 상태를 구분했다. 현재 Meta 앱 범위 사용자 ID를 Meta 계정명·이메일로 표현하지 않는다.
- 직원 상세 화면에 앱 범위 사용자 ID, 연령 범주, 모드, 시나리오, 완료 세션, 종료 세션 누적시간, PPE 잡기 시도와 PPE별 결과 연결시간·검사시간·판정 결과를 추가했다.
- 현재 이벤트에는 과제 시도 경계가 없으므로 잡기 시도 횟수는 표시하되 공식 재시도 횟수는 산정하지 않는다. Test 모드 식별과 전용 점수·합격 결과도 구분한다.
- 콘텐츠 품질 화면에 PPE별 참여 사용자 수를 추가해 단일 사용자 표본을 전체 설계 문제로 일반화하지 않도록 했다.
- 이탈 분석은 `완료 전 종료 후보 ÷ 종료 이벤트가 있는 세션`을 `완료 전 종료 후보율`로 표시한다. `application_quitting`은 앱 종료 콜백 기록으로만 표시하며 서비스 문제·개인 사정·EXIT Point 원인으로 변환하지 않는다.

### 데이터 활용 시점과 현재 계약

| 업무 질문 | 현재 활용 시점 | 현재 근거 | 확정 판단에 추가로 필요한 값 |
|---|---|---|---|
| 어느 단계가 오래 걸리는가 | 세션 종료 직후·주간 UX 리뷰 | 연속 `flow_state_changed`와 `session_ended` 시각 | 못 찾음 판정에는 대상 노출·첫 시선/호버·힌트 요청 |
| PPE 잡기 결과가 늦게 연결되는가 | 세션 종료 직후·배포 전후 | `ppe_grab_attempted`, `ppe_grab_attempt_resolved`, `attemptElapsedSec` | 장치 입력 지연 판정에는 입력·Interactor·Select 구간별 시각 |
| 어느 시나리오·PPE를 개선해야 하는가 | 다수 사용자·세션 누적 후 | 검사시간, 판정 결과, 잡기 결과, 사용자 표본 수 | 버전 태그, 동일 조건 비교, 실제 플레이 관찰 |
| 직원이 무엇을 학습·완료했는가 | 세션 종료 직후·월간 교육 운영 | 앱 범위 사용자 ID, 모드, 시나리오, `Completed`, PPE 이벤트 | 회원사·사번·부서, 조직별 수료 규칙 |
| 공식 Test를 통과했는가 | 현재 사용 불가 | Test 모드 문자열만 존재할 수 있음 | 문항·정오·점수·합격·재응시 계약과 실제 로그 |
| 업데이트로 개선됐는가 | 서로 다른 버전의 세션 누적 후 | 새 세션의 `appVersion`; 기존 13개 세션은 버전 미수집 | 콘텐츠 버전, 배포시점, 동일 모집단·모드·시나리오 필터 |
| 서비스 문제로 이탈했는가 | 원인 이벤트 연동 후 | 완료 전 종료 후보와 `application_quitting` | 오류·충돌·네트워크·종료 경로와 구조화된 사용자 종료 사유 |

### 근본 원인과 영향 범위

기존 화면은 실제 텔레메트리를 사람이 이해할 말로 바꿨지만, 기업이 수행하려는 업무 질문별 정보구조가 충분하지 않았다. 또한 현재 신호로 관찰 가능한 현상과 추가 계약 후 판정할 수 있는 원인을 한 화면에서 명확히 구분할 필요가 있었다.

변경 범위는 `server/public/dashboard/index.html`, `client/Assets/Scripts/PPETrainingTelemetryCapture.cs`, 대시보드 제공 테스트와 이 회의록이다. 텔레메트리에는 `session_started.appVersion`만 추가했으며 서버 API, 사용자 DB 스키마, 교육 상태 전이와 XR 입력 동작은 변경하지 않았다.

### Unity 변경 전 필수 질문 답변

1. 기존 Inspector/씬 작성값은 변경하지 않는다.
2. 버전 값의 소유자는 `PPETrainingTelemetryCapture.Record`, 기준 원본은 `Application.version`이다.
3. 입력 이벤트·Interactor·Caster·Raycaster·Layer·Collider 경로는 변경하지 않는다.
4. 버전 누락을 런타임에서 추정하거나 자동 수리하지 않는다. 기존 로그는 계속 미수집으로 남긴다.
5. 소비자는 로컬 JSONL과 대시보드 버전 집계뿐이며 UI·텔레포트·PPE Grab·거울·XR 양안 동작에는 영향을 주지 않는다.
6. 변경 전 기준은 기존 13개 세션에 버전 필드가 없는 상태다. 변경 후 비교 실행은 새 Unity 세션의 `session_started.appVersion` 확인이며 아직 실제 Play 로그가 필요하다.
7. 정적 코드·Unity 컴파일·계약 하네스까지 확인하고, Unity Play·Quest/OpenXR 새 세션 수집은 별도 수동 검증으로 남긴다.

### 완료한 검증

- 대시보드 JavaScript 구문 검사 통과
- 서버 자동 테스트 13건 통과
- 로컬 서버 health와 대시보드 HTTP 200 확인
- 실제 13개 세션의 이벤트 유형·필드와 대시보드 계산 입력을 대조
- Unity 명령 실행 시 C# 컴파일 성공
- `PPETrainingDataContractHarness.Validate()` PASS
- `DocumentationPolicyHarness.Validate()` PASS

### 아직 필요한 수동 검증

- 연결 가능한 브라우저 인스턴스가 없어 7개 메뉴 전환, 직원 세션 선택, 감사 로그 이동, 데스크톱·모바일 테이블 잘림은 수동 확인이 필요하다.
- 새 Unity Play/Quest 세션의 `session_started.appVersion`이 JSONL과 서버 조회에 나타나는지 확인해야 한다.
- 업데이트 비교, 회원사 조회, 공식 Test 결과, 원인별 이탈률은 서로 다른 버전 표본 또는 해당 데이터 계약을 구현하고 실제 Unity/Quest 로그와 서버 조회를 확인한 뒤에만 수치 검증할 수 있다.

## 회사·조직 정보 범위와 대시보드 방향 합의

### 현재 합의 범위

- 현재 단계에서는 대시보드의 최종 지표, 첫 화면 카드, 차트와 메뉴 구성을 확정하지 않는다.
- 클라이언트는 현재의 `flow_state_changed`, `ppe_choice_resolved`, `application_paused`처럼 화면과 무관하게 실제 발생한 업무 사실을 원본 이벤트로 기록한다. 특정 대시보드 화면, 위젯 또는 지표 하나만을 위한 이벤트는 추가하지 않는다.
- 기존 원본 이벤트에서 계산할 수 있는 지표, 분류, 필터, 차트와 경고는 서버가 파생한다. 대시보드 화면 변경만으로 Unity 클라이언트를 수정하지 않는다.
- 지표 하나와 이벤트 하나를 일대일로 대응시키지 않는다. 버전 비교는 `session_started.appVersion`처럼 세션에 기록된 원본 사실을 사용하고, 여러 지표가 같은 원본 이벤트를 재사용할 수 있게 한다.
- 기존 원본으로 계산할 수 없는 필수 사실이 실제 Unity 또는 Quest 실행에서 확인된 경우에만 클라이언트 계측 확장을 검토한다. 이때 개인정보, 저장량, 이벤트 의미, 기존 계약의 하위 호환성과 XR 입력·UI·상태 전이에 미치는 영향을 함께 검토한다.

### 회사·조직 정보와 외부 연동 경계

- 회사, 부서, 사번, 성명, 재직 상태와 교육 기수 같은 조직 정보는 Quest 클라이언트에서 수집하지 않으며 현재 텔레메트리 DB 계약에도 추가하지 않는다.
- 기업별 교육 이력과 공식 수료·평가가 필요해지면 앱 범위 사용자 ID와 권한 통제된 기업 직원 디렉터리를 서버에서 연결한다. 조직 정보를 기존 텔레메트리 원본으로 추정하거나 임의 값으로 채우지 않는다.
- 회사용 비즈니스 페이지와 Meta Business 연동은 현재 구현 범위에서 제외한다. 최종 API, 권한 모델, DB 스키마와 Meta Business 연동 방식은 실제 요구사항이 확정된 뒤 별도 계약으로 설계한다.
- 향후 회사 화면을 실제 연동 전 샘플 데이터 기반 목업으로 제작할 수는 있으나, 샘플과 실제 텔레메트리·운영 DB 데이터를 화면과 문서에서 명확히 구분한다.

### 현재 구현 사실과 미확정 사항

- 현재 서버는 허용된 이벤트를 `payload_json` JSON으로 보존하지만 클라이언트와 서버의 이벤트 필드 목록은 고정 계약이다. `eventType + eventSchemaVersion + payload` 형태는 향후 확장 제안이며 현재 구현 완료 사실로 표시하지 않는다.
- 현재 대시보드는 운영 텔레메트리 DB 집계 화면이 아니라 `/api/local-telemetry`가 제공하는 로컬 JSONL을 브라우저 JavaScript에서 계산하는 개발 확인 화면이고 운영 공개 대상이 아니다.
- 최종 대시보드 지표, 집계 API, 운영 DB 조회 계약, 회사·조직 권한 모델과 Meta Business 연동 방식은 미확정 상태로 유지한다.
- 현재 우선순위는 기존 클라이언트 이벤트의 안정적인 서버 적재와 Quest 환경의 실제 통합 검증이다. 대시보드용 신규 계측이나 회사 정보 연동을 이 검증보다 먼저 완료한 것으로 표시하지 않는다.

### 교차 저장소 기준

- 합의 확인 시점의 클라이언트 기준은 `main@77d51bec8faf9888f3fbf4396296bc6cdb447b96`이다.
- 서버 문서 변경 전 기준은 `main@4735992d5db4c8e54e6ee0c7594d31d70f55164a`이다.
- 이 절은 서버 미러에 먼저 반영하므로 클라이언트 기준본의 같은 상대 경로에 문서 동기화가 필요하다. 동기화 전에는 서버 문서 커밋과 클라이언트 구현 커밋을 서로 같은 완료 상태로 확대 해석하지 않는다.

## 2026-09-01 교육·훈련·테스트 모드 원본 계측 보완

### 근본 원인

- 기존 `PPETrainingTelemetryCapture`는 공통 행동 이벤트에 현재 `mode` 문자열만 기록했으며, Education·Training·Test 각각의 실행 시작과 완료 경계를 만들지 않았다.
- `PPEQuizController`와 `PPEVoiceFlowDirector`는 Test 정답 수, PPE 오답 수와 소요 시간을 메모리에서 계산해 결과 UI에만 사용했고 JSONL 또는 서버 이벤트로 전달하지 않았다.
- 따라서 기존 구현은 세 모드의 공통 행동을 구분할 수는 있었지만, 모드별 실행 횟수·완료 여부와 Test 평가 결과를 확정할 원본 계약은 아니었다.

### 적용 변경과 계산 규칙

| 원본 이벤트 | 적용 모드 | 기록 사실 |
|---|---|---|
| `mode_session_started` | Education·Training·Test | 모드 선택이 확정된 시점의 `modeSessionId`, `mode`, `workPlan` |
| `quiz_answer_resolved` | Education·Training·Test | 문항 순번·전체 문항 수·주제·선택지 순번·정오. Education·Training의 재선택은 실제 선택마다 기록하고 Test는 기존 첫 선택 확정 동작을 따른다. |
| `mode_session_completed` | Education·Training·Test | 같은 `modeSessionId`의 정답 수·전체 문항 수·PPE 오답 수·모드 소요 시간과 `result=completed` |

- `modeSessionId`는 한 앱 실행 안에서 반복되는 모드 실행을 서로 구분하는 32자 소문자 16진수 ID다. 앱 수명 단위 `sessionId`를 대체하지 않는다.
- 모드 실행 중 발생하는 기존 PPE 선택·잡기·검사·음성 이벤트에도 현재 `modeSessionId`를 붙여 시작·수행·평가·완료를 같은 실행으로 조회한다.
- Test 화면 점수는 기존 UI 규칙상 5문항의 정답 수에 20을 곱하지만, 서버 원본에는 파생 점수 대신 `quizCorrectCount`와 `quizQuestionCount`를 보존한다.
- 합격·불합격 기준은 제품에서 확정되지 않았으므로 이벤트나 서버에서 새로 판정하지 않는다.
- `application_quitting`은 계속 앱 종료 콜백이며 모드 완료로 사용하지 않는다.
- 서버는 새 필드를 기존 `payload_json`에 저장하므로 DB 마이그레이션은 필요하지 않다. 세 모드명, 모드 실행 ID, 문항 인덱스와 완료 집계의 형식을 수신 시 검증한다.

### 영향 범위와 검증 경계

- 클라이언트 반영 대상은 `PPEVoiceFlowDirector`, `PPEQuizController`, `PPETrainingTelemetryCapture`, `TycheTrainingTelemetryUploader`와 정적 계약 하네스다.
- 서버 반영 대상은 훈련 텔레메트리 이벤트 허용·검증 계약과 자동 테스트다. 등록 API, 사용자 API, DB 스키마, XR 입력, 음성, UI 표시와 모드 진행 규칙은 변경하지 않는다.
- 정적 C# 빌드와 서버 자동 테스트는 코드·계약 회귀 검증이다. 실제 Unity Play에서 세 모드를 각각 완료해 JSONL의 시작·문항·완료 이벤트가 같은 `modeSessionId`로 연결되고, 서버 상세 조회에서 sequence 누락·중복 없이 보이는지는 수동 통합 검증으로 남긴다.
- 변경 전 기준은 클라이언트 `main@fd925828f63eba564849eea47477a15325b22b7e`, 서버 `main@e20e59e5a1c209e66cda5b604f4815540d6a24a0`이다. 현재 변경은 양쪽 작업 트리에 반영된 상태이며 커밋 SHA는 아직 없다.

## 2026-09-01 모드·시나리오별 수행시간 기준 합의

### 측정 목적과 비교 단위

- 시간 데이터는 각 단계가 정상적으로 이행됐는지 확인하고, 사용자가 오래 머무는 구간을 찾아 음성 콘텐츠·UX·상호작용·상태 전환 중 어느 부분을 개선할지 판단하기 위해 사용한다.
- 현재 구현된 작업계획 `ConfinedSpace`, `LeakResponse`와 학습 모드 Education·Training·Test의 조합을 각각 독립된 기준으로 본다. 따라서 현재 기준선은 작업계획 2종 × 모드 3종의 6개 조합이며, 서로 다른 조합의 총시간을 하나의 평균으로 합치지 않는다.
- `전체 보이스 안내를 듣는다`는 모든 모드에서 같은 음성을 강제로 재생한다는 뜻이 아니다. 해당 작업계획과 모드에 설계된 음성을 생략·건너뛰기·중단 없이 끝까지 듣는 조건을 뜻한다. 모드별 음성 구성 차이도 기준시간의 일부다.
- 씬 파일명은 기술 식별자이며 작업계획·모드의 업무 의미를 대신하지 않는다. 씬 이름 변경 뒤에도 기준 집계 키는 `workPlan`, `mode`, `modeSessionId`를 사용한다.

### 기준시간과 회복시간의 분리

| 구분 | 포함 범위 | 비교 목적 |
|---|---|---|
| 기본 수행시간 | 해당 모드의 음성 안내, 안내 종료 후 첫 판단·행동까지, 첫 시도의 정상 수행, 정상 상태 전환 대기 | 작업계획·모드별 콘텐츠 길이와 기본 UX 병목 비교 |
| 회복시간 | 첫 오답·실패가 발생한 시점부터 정상 상태로 돌아오거나 해당 단계를 완료할 때까지 | 개인별 학습·숙련 과정과 도움 필요 구간 분석 |
| 전체 체류시간 | 기본 수행시간과 회복시간을 포함한 실제 경과시간 | 현장 운영시간과 세션 규모 참고 |

- 사람마다 틀리는 위치와 횟수가 다르므로 회복시간은 공통 기준시간에 합산하지 않는다. 공식 기준선은 숙련된 수행자가 각 조합을 첫 시도에 정상 완료한 실행으로 만들고, 동일 조건을 반복 측정할 때에는 중앙값을 사용한다.
- Education의 오답·재시도는 안내와 학습 효과, Training의 오답·재시도는 숙련 과정, Test의 최초 선택은 평가 원본으로 해석한다. 세 모드의 재시도를 같은 실패 횟수나 같은 시간 지표로 합산하지 않는다.
- Test는 현재 첫 선택을 확정하는 동작이므로 재시도 시간을 공식 평가 기준에 넣지 않는다. 합격·불합격은 별도 제품 기준이 확정되기 전까지 계산하지 않는다.
- 재시도 관련 보조 지표는 `첫 시도 성공 여부`, `최초 오답 단계 또는 PPE`, `회복시간`, `반복 오답 수`, `미완료 단계`, `음성 재생·도움 요청`으로 분리한다. 원본 이벤트나 계산 경로가 없는 항목은 미계측으로 표시한다.

### 음성 구간과 병목 계산 원칙

- 단계 체류시간은 음성 안내 구간, 음성 종료 후 첫 행동까지의 반응 구간, 실제 상호작용 구간, 정상 상태 전환 대기 구간으로 나누어 본다. 이를 각각 콘텐츠 병목, 반응 지연, 상호작용 병목, 시스템 전환 병목의 관측 신호로 사용하되 단일 신호만으로 원인을 확정하지 않는다.
- 음성과 사용자 행동이 겹칠 수 있으므로 여러 `voice_playback_*` 구간의 길이를 단계 체류시간에서 단순 합산·차감하지 않는다. 같은 시간축에서 겹치는 재생 구간은 합집합으로 계산하고, 행동 구간과의 중첩 여부를 함께 보존한다.
- 기준 실행에서도 음성 교체·중단·누락, 단계 이벤트 누락, 모드 실행 ID 불일치가 있으면 정상 기준 표본에서 제외하고 기술 검증 대상으로 분류한다.
- 작업계획·모드별 세부 병목을 비교하려면 음성 이벤트와 단계 이벤트가 같은 `modeSessionId`로 연결되어야 한다. 음성 파일명만으로 업무 단계를 추정하지 않으며, 안정적인 단계 식별자가 없는 구간은 상세 원인 분석을 미검증으로 둔다.

### Test 결과와 DB 반영 범위

- 서버의 현재 저장 범위는 기존 훈련 텔레메트리 이벤트 행의 `payload_json`이다. `mode_session_started`, `quiz_answer_resolved`, `mode_session_completed`와 모드 실행 중의 공통 행동 이벤트에 `modeSessionId`를 저장하며 새 테이블이나 컬럼은 추가하지 않는다.
- DB에 보존하는 Test 원본은 문항 순번, 선택지 순번, 정오, 정답 수, 전체 문항 수, PPE 오답 수, 모드 전체 경과시간이다. 화면 점수, 합격·불합격, 기본 수행시간, 회복시간과 병목 분류는 원본 또는 제품 기준에서 계산하는 파생값이며 현재 DB의 확정 컬럼이 아니다.
- 서버 자동 테스트는 세 모드 각각에 대해 모드 시작, 같은 `modeSessionId`가 붙은 공통 PPE 선택, 문항 결과, 모드 완료를 API에 저장하고 상세 조회로 되읽는 계약과 잘못된 모드 거부를 검증한다.
- 이 자동 테스트는 인메모리 저장소 기반이므로 MySQL 실제 INSERT·조회, 운영 DB 반영, Quest 업로드와 씬 연결 성공을 증명하지 않는다. DB 마이그레이션이 불필요하다는 것은 기존 `payload_json` 저장 구조를 재사용한다는 정적 구현 판단이며 운영 적재 완료를 뜻하지 않는다.

### 씬 이름 변경 후 클라이언트 인수인계 순서

1. 진행 중인 씬 이름 변경과 참조 경로 갱신을 먼저 완료하고, Build Settings·로딩 코드·에디터 하네스·씬 직렬화 참조가 새 이름을 가리키는지 검증한다.
2. 그 다음 이 문서의 `교육·훈련·테스트 모드 원본 계측 보완`과 본 절을 다시 읽고, 이전 씬 이름을 전제로 텔레메트리 연결을 수정하지 않는다.
3. 현재 씬과 런타임 코드에서 `mode_session_started`부터 공통 PPE·검사·음성 이벤트, `quiz_answer_resolved`, `mode_session_completed`까지 같은 `modeSessionId`가 유지되는지 확인한다.
4. `ConfinedSpace`, `LeakResponse` 각각에서 Education·Training·Test를 음성 생략 없이 완료해 총 6개 조합의 새 JSONL 표본을 만든다. 각 조합은 첫 시도 정상 완료 기준 표본과 재시도 포함 표본을 섞지 않는다.
5. 서버 상세 조회에서 이벤트 순서·누락·중복, `workPlan`, `mode`, `modeSessionId`, 문항 원본과 완료 집계를 대조한다. 이 실제 Play/Quest → JSONL → Express → DB 조회가 끝나기 전에는 통합 검증 완료나 기준시간 수치를 확정하지 않는다.

## 2026-09-03 정상 완료 경계 정정

### 이번 변경이 대응하는 사용자 요청

- 모드 실행 시작은 사용자가 Education·Training·Test 중 하나를 선택해 실행이 확정된 시점으로 유지한다.
- 정상 완료는 마지막 퀴즈 선택 시점이 아니라, 모드별 종료 음성과 복귀 전환이 끝나고 사용자가 다음 모드를 연속 선택할 수 있는 모드 선택 모달이 실제로 다시 표시된 시점으로 기록한다.
- EXIT Point를 통한 중도 복귀는 모드 선택 모달로 돌아오더라도 `mode_session_completed`로 기록하지 않는다.

### 변경 전 필수 판단

1. **기존 Inspector/씬 작성값 보존:** 이번 정정은 런타임 완료 이벤트 호출 순서만 대상으로 하며 씬, 프리팹, UI 배치, 오디오 클립과 Inspector 직렬화 값은 변경하지 않는다.
2. **단일 기준 오브젝트와 상태 소유자:** `PPEVoiceFlowDirector`가 `modeSessionId`와 계측 값을 소유하고, `PPEFinaleController`가 종료 음성 이후 복귀 순서를 소유한다.
3. **전체 경로:** 모드 선택 → `BeginModeSessionTracking()` → PPE·퀴즈 수행 → `NotifyQuizCompleted()` → 모드별 종료 음성 대기 → Test 결과 확인 또는 Education·Training 자동 복귀 → 암전·시작 위치 복귀·페이드인 → `ShowPpeModeChoicesAfterCompletion()` → 같은 ID의 `mode_session_completed` 기록 → 다음 선택을 위한 세션 초기화 순서다. 중도 EXIT는 별도의 미완료 복귀 경로를 사용한다.
4. **실패 처리:** 필수 모달 참조 또는 활성 모드 실행 ID가 없으면 완료를 자동 보정하거나 임의 생성하지 않고 명확한 오류를 남기며 완료 이벤트를 기록하지 않는다.
5. **영향 소비자:** 모드별 전체 체류시간, 완료율, 연속 모드 실행 분리와 서버·대시보드 집계가 영향을 받는다. XR 입력, 텔레포트 허용 규칙, PPE Grab, 음성 재생 순서와 UI 표현은 변경하지 않는다.
6. **변경 전후 비교 실행:** 변경 전에는 마지막 퀴즈 선택 직후 완료가 기록돼 종료 음성·결과 확인·복귀 시간이 빠졌다. 변경 후에는 모드 선택 모달 표시 이후 완료가 기록되고, 정상 완료 세 모드와 중도 EXIT를 각각 비교한다.
7. **검증 범위:** C# 빌드와 `PPETrainingDataContractHarness`, `PPETrainTestModeValidationHarness`는 정적 순서 계약만 확인한다. Unity Editor 실행과 Quest/OpenXR의 실제 모달 표시, JSONL 이벤트 순서 및 서버 적재는 별도 수동 검증으로 구분한다.

### 보존해야 하는 기존 동작

- Education·Training의 종료 음성 후 자동 복귀, Test의 결과 화면과 확인 후 복귀, 암전·페이드인, 장비·체크리스트 초기화와 모드 연속 선택 동작을 유지한다.
- 퀴즈 완료 시점에 계산하던 Test 결과 표시용 시간·정답 수·PPE 오답 수는 유지하되, 원본 완료 이벤트의 총 경과시간만 모달 복귀 시점에 다시 계산한다.
- 기준 수정 뒤 새 빌드에서 만든 표본만 정상 기준 데이터로 사용한다. 기존 빌드의 완료 시간과 섞지 않는다.

### 교차 저장소 상태

- 변경 전 클라이언트 기준은 `release/2026-09-01-meta-horizon-submission@9355cb85e9b88fcdd0af3bd398785bcc1e860698`이다.
- 확인한 서버 기준은 `main@238c7741d0f4f8c939bafdd9e4cc85dd6ea2bbcb`이다.
- 이 문서는 공용 문서이므로 클라이언트 변경·검증이 확정된 뒤 서버 저장소의 같은 상대 경로에 미러링해야 한다. 미러링 전에는 서버 반영 또는 통합 검증 완료로 표시하지 않는다.

### Train/Test 검증 하네스 씬 경로 정정

- `PPETrainTestModeValidationHarness`의 기본 검사 대상이 삭제된 과거 씬 `Assets/Scenes/3_PPE_Room_Train_Test.unity`로 남아 있어 `ArgumentException: Scene file not found`가 발생했다.
- `ProjectSettings/EditorBuildSettings.asset`의 현재 활성 PPE 씬과 동일한 `Assets/Scenes/4_PPE_Room.unity`로 기본 검사 경로를 정정했다.
- 이 변경은 검사 도구의 읽기 대상만 바로잡으며 씬 내용, 직렬화 참조, 런타임 상태와 UI·음성·입력 동작을 수정하지 않는다.
- 경로 정정 후 하네스는 상세 컨트롤러 안내의 과거 5개 연속 음원 번호를 기대해 현재 작성된 9개 피드백 구조의 정상 음원 `004_GripGrab_Release`, `006_Joystick`을 실패로 판정했다. 현재 씬과 음성 설계 문서의 `설명 → 입력 대기 → 003/005/007 오입력 → 008 정입력 → 009 완료` 구조에 맞춰 기본 설명 Clip 이름 기준만 정정했다.
- 상세 Joystick은 입력 전 설명 Clip이 `006` 하나이므로 상세 시각 배열도 한 칸이고, 간단 안내는 `004_Joystick → 005_GuideFollow` 두 Clip이라 같은 조이스틱 시각을 두 칸에 재사용한다. 하네스가 두 배열의 전체 길이까지 같아야 한다고 잘못 판정하던 조건을 `상세 설명 Clip마다 대응 시각이 있고, 같은 순번의 간단 안내 작성 시각을 재사용함`으로 정정했다.

### 2026-09-03 작업 종료 시 검증 상태

- `Tools > PPE > Validate Training Data Contract`는 현재 `4_PPE_Room` 기준 PASS다.
- 런타임과 Editor 보조 C# 빌드는 오류 0개였다.
- `PPETrainTestModeValidationHarness`의 씬 경로·음원 번호·상세/간단 시각 배열 조건은 현재 작성 구조로 수정했지만 마지막 시각 배열 조건 수정 뒤 Unity에서 재실행한 PASS는 아직 없다.
- `AndroidBundleVersionCode=4`는 설정됐지만 APK 빌드, Alpha 업로드와 Quest 설치는 아직 하지 않았다.
- 다음 재개 시에는 Train/Test 하네스 PASS를 먼저 확인하고 code 4를 빌드한 뒤, 한 작업계획의 Test → Training → Education 정상 완료 3회를 모드 선택 모달 복귀까지 수행한다. 그 전의 code 3 회차는 새 완료 경계의 기준 데이터로 사용하지 않는다.

## 2026-09-03 집 PC 재실행: Train/Test 하네스 회귀 판정

### 재현 결과와 오류 원문

- 집 PC에서 클라이언트 `main@4565b3fe241bdce5c40d01d267594d256d069ebe`를 pull하고 Unity
  `6000.4.8f1`의 `Tools > PPE > Validate Train Test Modes`를 실행했다.
- Console의 접힌 첫 항목에는 `1_Ray`의 `Card`·`Panel` 활성 조건만 보였지만 `Editor.log`에는 다음
  5개 실패가 같은 실행에 기록됐다.
  1. `The authored 1_Ray Card and Panel children must both remain active for detailed/simple guide reuse.`
  2. `Detailed controller education must mark completion and return to keyboard input.`
  3. `detailed Trigger lower image is not active after the simulated runtime presentation transition.`
  4. `detailed Joystick right guide '3_Ray_T' is missing.`
  5. `Simple Trigger lower image after return is not active after the simulated runtime presentation transition.`
- 이 실행은 정적·Preview Scene 하네스 실패이며 Play Mode, Android 빌드 또는 Quest/OpenXR 실기 결과가
  아니다. 집 PC에는 학원 PC의 APK와 HMD가 없으므로 설치된 실제 빌드 번호도 이번 재현으로 확정하지 않는다.

### 최근 변경 대조와 회귀 판정

- 현재 기본 씬 `Assets/Scenes/4_PPE_Room.unity`의 `1_Ray/Panel.activeSelf=false`,
  `3_Ray_T` 없음, `3_Exit_Marker` 존재, `m_SkipKeyboardNameInput=true`는 최신 pull 전의
  `506664f`, `518fca0`, `9355cb8` 직전 부모와 `1c3b24e`에서도 동일했다. 따라서 이 네 씬 작성값이
  `49be0d3`에서 새로 회귀한 것은 아니다.
- `49be0d3`은 `PPETrainTestModeValidationHarness`의 기본 검사 대상을 삭제된 과거 씬에서 현재
  `4_PPE_Room`으로 정정했지만, Preview Scene 검사는 여전히 과거 `3_Ray_T` 이름과 키보드 활성 시절의
  `Controller Edu → NameInput` 전이를 요구한다. 검사 대상만 현재 씬으로 바뀌고 내부 기대값이 끝까지
  동기화되지 않아 최신 기준에서 필연적으로 FAIL하는 **검증 하네스 회귀**로 판정한다.
- 현재 출시 흐름은 `m_SkipKeyboardNameInput=true`이므로 Welcome 뒤 `Simple`로 진입하고, Simple 중 A로
  들어간 상세교육이 끝나면 비활성 키보드가 아니라 `CardIntro`로 진행하는 것이 최신 확정 동작이다.
  런타임의 `ResolveControllerEducationNextState()`는 이 우회 규칙을 적용하고 있으므로 두 번째 오류를
  해결하기 위해 런타임 상태 전이를 과거 `NameInput` 복귀로 되돌리지 않는다.
- Joystick 오른쪽 가이드의 현재 씬 기준은 `3_Exit_Marker`다. 네 번째 오류를 없애기 위해 삭제된
  `3_Ray_T` 오브젝트를 재생성하거나 현재 씬 오브젝트를 이름 변경하지 않는다. 하네스가 현재 직렬화 참조와
  이름을 검사하도록 고쳐야 한다.
- `1_Ray/Panel.activeSelf=false`는 최근 pull 회귀는 아니지만, 현재 상세·Simple Trigger 단계에서 하단
  이미지가 보이지 않게 할 수 있는 실제 씬 불일치 후보다. 첫 번째·세 번째·다섯 번째 오류는 이 한 작성값에서
  파생된다. 과거 문서의 동시 표시 요구와 현재 Inspector 작성값이 충돌하므로, 실제 Game View 기준 표시와
  사용자 의도를 확인해 Inspector에서 활성 여부를 확정하기 전에는 자동 수정하지 않는다.

### OpenXR Import Error Code 4 분리

- 같은 Unity 세션에서 `Assets/XR/Settings/OpenXR Package Settings.asset`의 SourceAssetDB 시각과 디스크
  수정 시각이 다르다는 `Import Error Code:(4)`가 한 번 기록됐다.
- 확인 당시 해당 파일의 Git index와 디스크 SHA-1은 모두
  `f167e4990ab0ef4a26201d3b6b866e261cfda848`이고 텍스트 diff도 없었다. pull 직후 병렬 Import 중 발생한
  시각 불일치로 분류하며 Train/Test의 위 5개 계약 실패와 합치지 않는다.
- Refresh 뒤 같은 오류가 반복되면 OpenXR AssetDB가 안정적으로 재임포트됐는지 먼저 확인한다. 반복 여부를
  확인하지 않은 상태에서 OpenXR 설정을 재저장하거나 Android 빌드를 시작하지 않는다.

### 영향 범위와 후속 검증

- 이번 조사에서는 런타임 코드, 씬, Inspector 값과 OpenXR 설정 내용을 수정하지 않았다.
- 후속 수정 범위는 `PPETrainTestModeValidationHarness`의 Joystick 오브젝트 기대값과 키보드 우회 전이
  기대값이다. `1_Ray/Panel`은 UI 작성값이므로 사용자 확인 뒤 Unity Inspector에서만 결정한다.
- 수정 후 `Tools > PPE > Validate Train Test Modes`를 다시 실행해 PASS를 확인하고, 같은 씬에서 상세와
  Simple Trigger의 상단·하단 이미지 및 Joystick 가이드를 Game View로 비교한다.
- Quest/OpenXR 양안 표시, 물리 A 재진입, `Simple → 상세교육 → CardIntro`, Android code 4 설치 상태와
  JSONL 완료 경계는 학원 PC와 HMD에서 별도 검증한다. 이 검증 전에는 code 4 실기나 정상 기준 데이터 수집을
  완료로 기록하지 않는다.

### 교차 저장소 기록 상태

- 조사 시점 서버 기준은 `main@d6b5963f7fef139588b6999731f6169cab4037ce`이며 작업 트리는 clean이었다.
- 이 절은 공용 사실 인수인계를 위해 서버 저장소의 같은 상대 경로에 미러링한다. 서버 런타임·API·DB 코드는
  변경하지 않았고, 문서 미러링을 서버 반영 또는 클라이언트·서버 통합 검증 완료로 해석하지 않는다.

## 2026-09-04 `main` 전환 후 과거 상태 오인 원인 확정

### 조사 요청과 보존 범위

- 사용자가 클라이언트·서버 최신 `main`을 pull한 뒤 Train/Test 후속작업을 확인하는 과정에서, 현재 씬과
  다른 과거 하네스 기대값이 발견된 이유와 브랜치 전환에 의한 롤백 여부를 확인하도록 요청했다.
- 이번 조사는 Git 이력, 현재 씬 YAML, 런타임 상태 전이와 하네스 코드를 대조하는 진단만 수행했다.
  런타임 코드, 씬, Inspector 작성값, 입력·UI·음성 동작과 서버 코드는 변경하지 않는다.

### Git 이력 대조 결과

- 조사 기준은 클라이언트 `main@cdde25c1d71cd7c7be81b5f722e89fa19c102b28`, 서버
  `main@72e82a16334136753d152704765f395e61819fcd`이며 양쪽 작업 트리와 현재 `origin/main` 추적점은
  일치했다.
- 제출 브랜치 `release/2026-09-01-meta-horizon-submission@49be0d36bc41a9e251c459395af0073667b2eba8`은
  `main`에 정상 병합돼 있다. 해당 브랜치와 조사 시점 `main` 사이에서
  `PPETrainTestModeValidationHarness.cs`, `PPEVoiceFlowDirector.cs`, `4_PPE_Room.unity`와
  `ProjectSettings.asset`의 차이는 없고, 차이는 본 후속 회의록의 추가 기록뿐이었다.
- 로컬 reflog에는 2026-09-04 브랜치 이동이 `checkout`으로만 기록돼 있고 `reset`, `revert` 또는 과거
  커밋으로의 강제 이동은 없다. 따라서 `main` 전환으로 클라이언트 코드나 씬이 과거 상태로 돌아간 것은 아니다.

### 근본 원인

- 불일치는 새 클라이언트 저장소 최초 스냅샷
  `45f662735eca25df5122d761b2466c13a7d12cfa`부터 존재했다. 당시 실제 PPE 씬은 이미
  `m_SkipKeyboardNameInput=1`, `3_Exit_Marker`, 비활성 `1_Ray/Panel`을 포함했지만, 같은 스냅샷의
  하네스는 상세교육 완료 후 `NameInput` 복귀, `3_Ray_T` 존재와 `1_Ray/Card`·`Panel` 동시 활성을
  요구했다.
- 2026-09-03 이전 하네스의 기본 대상은 삭제된 과거 씬
  `Assets/Scenes/3_PPE_Room_Train_Test.unity`였다. 검사가 씬 누락에서 먼저 중단돼 뒤쪽의 오래된
  `NameInput`, `3_Ray_T`, `Panel` 조건이 실제 현재 씬을 상대로 실행되지 않았다.
- `49be0d3`에서 기본 대상을 `Assets/Scenes/4_PPE_Room.unity`로 변경하고 음원 번호와 시각 배열 조건은
  현재 구조에 맞췄지만, 위 세 과거 기대값은 함께 정정하지 못했다. 검사 대상 복구로 가려졌던 조건이 처음
  노출되면서 롤백처럼 보인 것이며, 실제 분류는 **검증 하네스 동기화 누락**이다.

### 영향 범위와 수정 경계

- `m_SkipKeyboardNameInput=1`일 때 상세교육 완료 후 `CardIntro`로 진행하는 현재 런타임 규칙과
  `3_Exit_Marker` 씬 작성값을 과거 하네스에 맞춰 되돌리지 않는다.
- 후속 코드 수정 범위는 `PPETrainTestModeValidationHarness`의 키보드 우회 완료 상태와 Joystick 오른쪽
  가이드 이름 기대값으로 제한한다.
- `1_Ray/Panel.activeSelf=false` 역시 최초 스냅샷부터 존재한 씬 작성값이다. 이 값은 단순 하네스 이름
  정정과 달리 실제 상세·Simple Trigger 하단 이미지 표시를 바꿀 수 있으므로, Game View에서 사용자 의도를
  확인한 뒤 Unity Inspector에서 결정한다. 하네스 통과를 위해 씬 또는 런타임에서 자동 활성화하지 않는다.

### 완료한 정적 검증과 남은 검증

- 클라이언트 런타임 C# 프로젝트는 NuGet 복원 후 오류 0개로 빌드했고, Editor C# 프로젝트도 오류 0개로
  빌드했다. 서버 `npm test`는 74개 테스트가 모두 통과했다.
- 정적 빌드는 Unity Preview Scene 하네스, Play Mode 상태 전이, Game View 시각, Quest/OpenXR 양안과
  code 4 APK 동작을 증명하지 않는다.
- 하네스 기대값을 현재 작성 상태에 맞춰 제한적으로 수정한 뒤 `Tools > PPE > Validate Train Test Modes`를
  재실행해야 한다. 이어서 상세·Simple Trigger 이미지와 Joystick 가이드를 Game View에서 비교하고,
  code 4 APK 빌드·Alpha 설치·Quest 실기와 새 완료 경계 JSONL 검증을 별도로 수행한다.

### 2026-09-04 하네스 수정 전 필수 질문 답변

1. **기존 Inspector/씬 작성값 보존:** `4_PPE_Room.unity`, 프리팹과 Inspector 값은 수정하지 않는다.
   하네스가 현재 직렬화·런타임 계약을 읽어 검증하도록 기대값만 정정한다.
2. **단일 기준 오브젝트와 상태 소유자:** 상태 전이는 `PPEVoiceFlowDirector`가 소유하며, Joystick 오른쪽
   가이드의 작성 기준은 `4_PPE_Room.unity`에 직렬화된 `3_Exit_Marker`다. 하네스는 소유자가 아니라
   이 두 기준을 검사하는 소비자다.
3. **입력·상태 전체 경로:** Simple 안내 중 물리 A 입력 → 상세교육 `ControllerRay` →
   `ControllerMarker` → `ControllerRayT` → `ResolveControllerEducationNextState()` →
   `ResolveKeyboardNameInputBypass(NameInput)` → `m_SkipKeyboardNameInput=1`이고 교육 완료이므로
   `CardIntro`로 진행한다. Joystick 시각은 상세 단계의 직렬화된 companion visual과
   `3_Exit_Marker` 활성 여부를 Preview Scene에서 검사한다.
4. **실패 처리:** 누락된 메서드·필드·씬 오브젝트를 하네스가 생성하거나 자동 수정하지 않는다. 현재 이름이나
   상태와 다르면 구체적인 오류로 실패한다.
5. **영향 소비자:** Editor의 Train/Test Preview Scene 검증만 영향을 받는다. 런타임 상태, XR 입력,
   텔레포트, PPE Grab, UI·음성 재생과 서버 계약은 변경하지 않는다.
6. **변경 전후 비교 실행:** 변경 전 Unity 배치 하네스에서 5개 실패를 재현했다. 변경 후에는 확정된
   `NameInput`·`3_Ray_T` 두 실패가 제거되는지 먼저 확인하며, `1_Ray/Panel`에서 파생된 3개 실패는
   시각 의도 확인 전까지 별도 미해결 상태로 유지한다.
7. **실제 검증 범위:** 이번 단계는 C# 정적 빌드와 Unity Editor 배치 하네스까지 수행한다. Game View 표시,
   Play Mode의 물리 A 재진입, Quest/OpenXR 양안과 code 4 APK는 후속 수동 검증으로 구분한다.

### Panel 표시 의도 추가 대조와 판정

- 확정된 `CardIntro`와 `3_Exit_Marker` 기대값을 먼저 수정한 뒤 Unity 배치 하네스를 재실행했다. 기존
  5개 실패 중 두 오류는 사라졌고 `1_Ray/Panel` 활성 요구에서 파생된 3개만 남아 수정 범위가 분리됐다.
- 현재 출시 체크리스트는 Simple Trigger에서 `1_Ray`와 컨트롤러 이미지를 표시하되 삭제된 `Panel`,
  `3_Ray_T`, `Ray_T_B/R`은 다시 보이면 안 된다고 명시한다. 현재
  `PPELocomotionPpeRegressionValidationHarness`도 `1_Ray/Card.activeSelf=true`,
  `1_Ray/Panel.activeSelf=false`, `m_ControllerRayTStep=3_Exit_Marker`, `m_ControllerPanelStep=null`을
  검사한다.
- 따라서 `Panel`의 현재 비활성 작성값은 미확정 UI 결함이 아니라 최신 출시 기준이다. Train/Test 하네스가
  과거 동시 표시 구조를 계속 요구한 것이 남은 세 실패의 원인이다.
- 씬이나 런타임에서 `Panel`을 다시 활성화하지 않는다. 하네스는 `Card`가 표시되고 `Panel`은 상세·Simple
  Trigger 전환 뒤에도 숨김 상태를 유지하는지 적극적으로 검사하도록 정정한다.

### 2026-09-04 입점 준비 검증 결과

- 변경 전 `PPETrainTestModeValidationHarness.ValidateBatch` 기준 실행에서 예상한 5개 실패를 재현했다. `CardIntro`와
  `3_Exit_Marker` 계약을 반영한 중간 실행에서는 `Panel` 관련 3개 실패만 남아 원인이 분리됐다.
- 최신 출시 체크리스트와 `PPELocomotionPpeRegressionValidationHarness`를 대조해 `1_Ray/Card`는 활성,
  삭제 대상인 `1_Ray/Panel`은 비활성이 현재 출시 계약임을 확인했다. 씬이나 런타임 UI를 바꾸지 않고
  `PPETrainTestModeValidationHarness`의 낡은 기대값만 현재 계약에 맞췄다.
- `dotnet build Assembly-CSharp-Editor.csproj`는 오류 0개로 완료됐다. 경고 68개는 기존 런타임 의존성과
  임포트 샘플 및 DTO 필드에 있으며 이번 변경에서 새 컴파일 오류는 발생하지 않았다.
- Unity `6000.4.8f1` batch mode에서 다음 검증을 실제 실행했다.
  1. `PPETrainTestModeValidationHarness.ValidateBatch`: PASS
  2. `PPETrainingDataContractHarness.ValidateBatch`: PASS
  3. `PPELocomotionPpeRegressionValidationHarness.ValidateBatch`: PASS
  4. `MetaQuestAndroidBuildValidationHarness.Validate`: PASS
- Android 정적 계약은 Target SDK 34, 가로 방향, 자동 설치 위치, Android OpenXR의 Meta Quest Support 활성까지
  확인했다. 아직 새 APK의 manifest에 `android.hardware.vr.headtracking`과 `com.oculus.intent.category.VR`가
  실제 포함되는지는 확인하지 않았다.
- **남은 수동 검증:** code 4 APK/AAB 빌드, 생성물 manifest 검사, Meta Quest 단독 설치·실행, 양안 렌더링,
  XR Trigger와 오디오, Education·Training·Test 정상 완료 후 모드 선택 모달 복귀, 최신 JSONL 및 서버 적재 확인은
  별도 증거가 필요하다. 이 항목들은 정적 하네스 PASS만으로 완료 처리하지 않는다.

## 2026-09-12 일반 사용자 VR 병목 대시보드 범위 결정

### 제품 대상과 1차 범위

- 현재 앱은 Meta Horizon에서 일반 사용자를 대상으로 배포한다. 따라서 1차 대시보드는 기업 직원
  평가·사번·부서·교육 대상 명단·미이수자·수료증 관리가 아닌 **일반 사용자 유입·반복 이용·VR 병목
  분석**에 집중한다.
- 기업용 Business Dashboard는 조직, 교육과정, 직원 명단, 수료 기준과 별도 동의·계약이 확정된 뒤의
  후속 제품으로 분리한다. 현재 화면에 수집하지 않는 직원 속성이나 직원 평가 결과가 존재하는 것처럼
  표시하지 않는다.
- 일반 사용자의 동일인 연결은 Meta 앱 범위 사용자 ID를 서버의 `training_telemetry_identities`에서
  내부 `participantId`로 변환하는 현재 계약을 사용한다. 대시보드에는 Meta ID 전체 대신 `사용자 #23`
  또는 마스킹된 앱 ID를 표시한다. 연령대, 사용자 프로필, 이름, 사번과 소속은 1차 범위에 추가하지
  않는다.

### 첫 화면의 질문과 지표

- 첫 화면은 `사용자가 늘고 있는가`, `다시 플레이하는가`, `어디에서 막히는가`를 한번에 답해야
  한다. 사용 현황·증가세 40%, 주요 병목 40%, 최근 플레이·데이터 상태 20% 정도의 정보 비중을
  기본으로 한다.
- 기간 필터는 `오늘`, `7일`, `30일`, `직접 설정`을 제공하고 모드, 시나리오, 앱 버전을 같이 필터링한다.
- 상단 핵심 지표는 `신규 관측 사용자`, `활성 사용자`, `재방문율`, `총 플레이`, `완료 플레이 비율`로
  구성한다. `신규 사용자`는 Meta 계정 생성이 아니라 텔레메트리에서 처음 관측된 `firstSeenAt`을
  기준으로 하므로 `신규 관측 사용자` 또는 `첫 훈련 사용자`로 표시한다.
- 사용자 유입 추이는 일·주 단위 선 그래프로 표시한다. 그래프는 신규 관측 사용자, 재방문 사용자,
  전체 플레이 추이를 같이 비교할 수 있게 한다. 이전 동일 길이 비교 기간이 없으면 임의 증감율을 만들지 않고
  `비교 기간 데이터 부족`과 원래 건수를 표시한다.
- Meta 앱 범위 ID만으로는 스토어 검색, 추천, 광고 등 유입 채널을 판정할 수 없다. 유입 **수**와 유입
  **경로**를 구분하고, 유입 경로는 실제 Meta Store 분석 데이터와 연동되기 전에 확정 지표로 표시하지
  않는다.

### 전체 사용자 병목 순위

- 대시보드의 대표 기능은 전체 사용자가 반복적으로 막히는 구간을 `단계별`과 `PPE별`로 나누어 순위화하는
  것이다. 두 범주를 한 순위에 섞지 않고 탭 또는 별도 보기로 구분한다.
- 정렬 기준은 `영향받은 고유 사용자 수` → `해당 구간에서 끝난 미완료 플레이 수` →
  `실패·오선택 발생 플레이 수` → `중앙 체류시간` → `총 재시도 횟수`를 우선으로 한다. 특정 사용자의
  반복 입력이 전체 순위를 왜곡하지 않도록 총 실패 횟수보다 영향 고유 사용자 수를 앞에 둔다.
- 단계별 병목은 진입 사용자, 다음 단계로 전환하지 못한 사용자, 마지막 도달 단계 횟수, 중앙·최대
  체류시간을 표시한다. PPE별 병목은 PPE 시도 사용자, 잘못 사용·거부한 사용자, Grab 실패
  사용자, 평균 재시도 횟수와 검사–판단 소요시간을 표시한다.
- 순위 항목은 원인을 확정하는 `문제 원인`이 아니라 `병목 후보`로 표시한다. 상세 화면은
  `병목 후보 → 영향 사용자 → 해당 플레이 → 단계 타임라인 → 원본 이벤트`로 이어진다.
- 한 사용자만의 반복 문제는 개인 재도전 후보, 여러 사용자의 동일 구간 반복은 콘텐츠·배치·안내·
  입력 병목 후보로 구분한다. 시간이 길어도 재시도·오선택·미완료가 없으면 즉시 문제로 확정하지 않는다.

### 병목 후보 검토·제외·해결 이력

- 자동 집계 결과는 `신규 후보 → 검토 중 → 병목 확인 / 관찰 유지 / 후보 제외 → 개선 완료`의
  검토 상태를 가진다. 병목 상세에서 관리자가 영향 세션과 원본 이벤트를 확인한 뒤에만 상태를
  변경한다.
- 후보 제외 이유는 표본 부족, 의도적으로 긴 교육 구간, 테스트·개발 플레이, 데이터 수집 오류, 특정
  사용자에게만 발생, 이미 수정된 구버전 문제, 중복 후보와 자유 기재 사유로 구분한다. 제외할 때는 범위,
  이유, 검토자, 검토 시각을 필수로 기록한다.
- 제외 범위는 전체 버전, 특정 앱 버전, 특정 모드·시나리오, 일정 기간 숨김을 구분한다. 새 앱
  버전이 배포되거나 영향 사용자 수가 기준을 넘으면 해당 제외 항목을 자동 재검토 후보로 복귀시키는
  정책을 둔다.
- 후보 제외은 병목 순위와 알림에서만 숨기고 원본 이벤트, 사용자 수, 완료율 등의 기초 통계를
  삭제하거나 다시 계산하지 않는다. 관리자의 판정은 별도 검토 레이어와 감사 이력으로 보존한다.
- `개선 완료`는 수정 전 버전·기간·지표, 수정 내용, 수정 후 버전·기간·지표, 영향 사용자 비율·
  체류시간·실패율 변화와 판정일을 근거로 기록한다.

### 기준 데이터와 비교 규칙

- 현재 확인된 Build 8 실제 Quest 세션은 **기술 검증 기준 데이터**로 사용한다. Quest 원본 생성,
  운영 HTTPS 업로드와 MySQL 저장이 대조된 세션 2개이며, `0ec97178…` 302개와 `9db5c193…`
  195개를 합해 원본 이벤트는 497개다. Education·Training·Test 완료 기록을 포함한다.
- 위 2개 세션은 API, 파서, 요약 계산과 상세 추적이 같은 원본에서 일치하는지 확인하는 기술 기준이지,
  일반 사용자 전체의 행동을 대표하는 운영 KPI 기준이 아니다. 표본이 적은 상태에서 완료율·병목 순위·사용자
  증가세를 일반화하지 않는다.
- 실제 일반 사용자가 쌓이면 `기준 기간`, `앱 버전`, `유효 사용자·세션 집합`, `제외 규칙`,
  `계산 규칙 버전`을 함께 고정한 **운영 비교 기준**을 별도로 생성한다. 기준선을 사후에 임의로 변경하지
  않고, 수정 전·후 동일 조건을 비교한다.
- 비교 화면은 신규·활성·재방문 사용자, 사용자당 플레이, 완료 플레이 비율, 단계별 중앙
  체류시간, 병목 영향 사용자 비율, PPE 오선택·Grab 실패 사용자 비율을 `기준 값 → 현재 값 →
  증감`으로 표시한다. 백분율은 반드시 원래 건수와 모수를 함께 표시한다.

### 현재 구현과 후속 경계

- 서버 코드에는 운영 MySQL 원본을 읽는
  `GET /api/training-telemetry/sessions`, `GET /api/training-telemetry/sessions/{sessionId}`,
  `GET /api/training-telemetry/participants`, `GET /api/training-telemetry/participants/{participantId}`가 존재한다.
- 현재 `public/dashboard/dashboard.js`는 운영 API가 아닌 `/api/local-telemetry`의 JSONL 파일을 읽는다. 또한
  운영 training telemetry 조회는 개발용 bearer token 경로로 보호되어 브라우저 관리자 세션과 직접 연결되지
  않았다. 따라서 현재 화면을 운영 대시보드 연결 완료로 표시하지 않는다.
- 1차 구현은 새 XR 수집 항목이나 Meta 권한을 추가하지 않고, 관리자 로그인 세션으로 운영 API를 안전하게
  조회하는 서버 경로를 먼저 연결한다. 이후 `사용자 유입 선 그래프 → 병목 순위 → 사용자·플레이 상세 →
  원본 이벤트 → 후보 검토·제외 이력`의 순서로 연결한다.
- 이 절은 제품 범위와 계산·검토 규칙을 기록한 것이며 대시보드 운영 API 연결, 집계 쿼리, 검토 상태
  저장, 화면 변경은 아직 구현·배포하지 않았다.

## 2026-09-13 첫 화면 로컬 구현과 교차 저장소 후속 작업

### 기준과 적용 변경

- 확인한 기준은 클라이언트 `origin/main@0a0e2277000e9033d43d2c453c48f9789e45cd0a`, 서버
  `origin/main@79f1ad8c12b65594cbe084b8eb6afc983b5d12a9`이다. 아래 첫 화면 변경은 서버의
  `codex/vr-dashboard-first` 작업 브랜치에 적용한 것으로, 운영 배포 상태가 아니다.
- 기존 첫 화면은 항목이 많아 신규 유입과 재방문의 핵심 흐름을 바로 읽기 어려웠다. 서버에
  `GET /api/training-telemetry/dashboard-overview?days=7|30`을 추가하고 관리자 세션으로 보호했다.
  조회는 `training_telemetry_sessions`와 `training_telemetry_events`의 저장값을 사용한다.
- `public/dashboard/index.html`을 핵심 현황 화면으로 바꾸고 기존 상세 화면을
  `public/dashboard/details.html`에 보존했다. 신규 관측·재방문 사용자는 일별 선그래프로,
  Education·Training·Test의 시작·완료는 별도 막대로 표시한다. 화면의 한국어 명칭은
  **교육·훈련·테스트**이며 기존 상세 화면의 동일 표기도 바로잡았다. `Test`를 `평가`로 바꾸지 않는다.
- `신규 관측`은 저장된 첫 세션 시작일이 조회 기간에 속하는 내부 참가자 ID 수다. 일별 `재방문`은
  첫 세션 날짜보다 나중에 다시 플레이한 참가자 ID 수다. 기간 내 `관측 사용자`는 해당 기간에
  세션이 있는 서로 다른 참가자 ID 수이며 실제 사람 수와 같다고 단정하지 않는다. 날짜 경계는 UTC다.
  모드 시작·완료 값은 해당 이벤트의 건수이지 이탈자 수나 완료율이 아니다.

### 클라이언트에서 확인한 작업과 검증 경계

- 클라이언트 `main@73ba5a1`에는 Meta 사용자 연령대 조회·전송을 제거한 코드 변경이 있다.
  `origin/main`의 `MetaPlatformIdentityProbe.cs`는 앱 범위 사용자 ID 조회를 유지하며
  `UserAgeCategory.Get()` 호출은 없다. 이것은 코드 확인이며 새 Production 빌드 반영·Meta 승인을
  뜻하지 않는다.
- 클라이언트 `main@beccf9f`, `main@46d2d3f`의 기존 검증 문서에는 Quest Build 8의
  Education·Training·Test 완료, 원본 이벤트의 서버 적재 대조와 제한 사항이 기록돼 있다.
  이번 작업에서 Quest를 다시 실행하거나 최신 빌드의 이벤트 수집을 재검증하지 않았다.
- 클라이언트 `main@85f9ff6`는 재심사용 빌드 번호를 올렸고, 기존 인수인계 문서에는 Alpha
  Build 9와 Production 제출 잠금 상태가 기록돼 있다. 클라이언트 코드 변경, Alpha 업로드,
  Store 승인과 서버 화면 조회를 하나의 완료 상태로 합치지 않는다.

### 로컬 확인과 남은 검증

- 로컬 미리보기는 운영 DB의 읽기 전용 **집계 스냅샷**을 저장소 밖 임시 파일에서 제공했다.
  원본 이벤트·사용자 ID를 문서나 Git에 넣지 않았다. 이는 조회 당시의 데이터로, 실시간 운영
  연결이나 신규 사용자 전체를 대표하는 표본이 아니다.
- 서버 `npm test` 115개 통과, 첫 화면 JS 구문 검사 통과, 로컬 HTTP에서 수정된
  `교육·훈련·테스트` 자산 제공을 확인했다. 실제 브라우저 렌더, 운영 관리자 로그인 조회,
  운영 배포와 새 Quest 이벤트의 화면 반영은 아직 확인하지 않았다.
- 후속 작업은 (1) 운영 승인 후 관리자 세션으로 API·화면과 원본 이벤트를 대조하고,
  (2) 실제 세션의 시작·종료·모드 이벤트로 기간별 분모와 미완료 상태를 검증한 뒤,
  (3) 사용자·플레이 상세와 원본 이벤트로 이어지는 병목 후보를 설계하며,
  (4) 추가 XR 수집이 정말 필요한 지표만 클라이언트 계약과 함께 정하는 순서로 진행한다.
  `application_quitting`만으로 이탈 원인을 확정하지 않는다.
