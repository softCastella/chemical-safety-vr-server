# 브랜드 홈페이지 및 VR 상세페이지 레이아웃 정리

- 작성일: 2026-08-26
- 대상 프로젝트: `Final_VR_Tyche_Pivot`
- 적용 범위: `server/public/site`

## 1. 작업 목적

브랜드 홈페이지를 기업 담당자와 일반 방문자가 함께 이해할 수 있는 홍보 페이지로 정리하고, 현재 제작 범위인 PPE 착용 교육 중심의 화학물질 안전훈련 VR 콘텐츠가 페이지 전반에서 일관되게 보이도록 구성한다.

## 2. 적용한 변경

### 홈페이지

- `Now on Tyche` 영역에 화학물질 안전훈련 VR 빅사이즈 배너를 전체 카드 기준으로 배치했다.
- VR 배너 왼쪽 정보 영역의 구성과 기존 배너 형식을 맞췄다.
- 문의 폼에 `문의 종류` 드롭다운을 추가했다.
  - 일반 문의
  - 비즈니스 문의
- 문의 폼의 입력 요소 스타일을 드롭다운까지 일관되게 적용했다.

### IMMERSA 페이지

- CTA 왼쪽 이미지 영역에 화학물질 안전훈련 VR 스퀘어 배너를 적용했다.
- 히어로 영역은 기존 VR 대표 이미지 구성을 유지했다.

### 화학물질 안전훈련 VR 상세페이지

- 상단의 `프로젝트 소개` 버튼을 제거했다.
- 상단 히어로 영역의 왼쪽에는 다음 정보를 배치했다.
  - `TYCHE IMMERSA / PROJECT 01`
  - `화학물질 안전훈련 VR`
  - PPE 확인·착용 교육 중심의 설명
- 오른쪽 이미지 바깥 위쪽에 프로젝트 뱃지를 배치했다.
- 뱃지는 오른쪽 정렬, 그림자 제거, 흐린 테두리 중심으로 정리했다.
- Meta Horizon 안내 문구와 카드는 이미지 아래에 유지했다.
- 히어로 하단에 얇고 흐린 그레이 구분선을 추가했다.
- `ppe_room`, `check_work`, `select_ppe`, `ppe_check`, `check_ppe` 에셋을 활용해 VR 교육 흐름을 소개했다.
- 페이지 하단에 문의하기 CTA를 유지했다.

### SPARK / LOOP 페이지

- 중앙 정렬 준비중 콘텐츠의 키커, 제목, 설명, 홈 이동 버튼 사이의 세로 간격을 넓혔다.
- 두 페이지에 공통 스타일로 적용했다.

### 사이트 타이포그래피

- 홈페이지, IMMERSA/VR, SPARK/LOOP, BRAND V2의 작은 라벨·상태·내비게이션·푸터 텍스트에 최소 12px 기준을 적용했다.
- 상세페이지의 작은 캡션과 히어로 뱃지도 12px로 맞췄다.

## 3. 표현 기준

- 현재 콘텐츠 범위는 PPE 착용 교육이다.
- 제작 중이 아닌 밀폐공간 교육이나 일반적인 안전훈련 전반을 현재 제공 범위처럼 표현하지 않는다.
- 방문자가 실제 체험 요소와 교육 흐름을 빠르게 파악할 수 있도록 이미지와 짧은 설명을 교차 배치한다.

## 4. 영향 범위

- 정적 브랜드 홈페이지와 관련 하위 페이지의 HTML/CSS 및 이미지 배치만 변경했다.
- Unity 클라이언트 런타임, XR 입력, 씬, 서버 API 동작은 변경하지 않았다.

## 5. 완료한 검증

- 수정 대상 HTML/CSS 파일의 구조와 선택자 적용 여부를 정적으로 확인했다.
- `git diff --check`를 실행했으며 작업 대상 파일에서 오류가 발생하지 않았다.

## 6. 아직 필요한 수동 검증

- 실제 브라우저에서 데스크톱·모바일 해상도의 히어로 간격과 뱃지 줄바꿈 확인
- 홈페이지 배너 전환 및 문의 폼 드롭다운 동작 확인
- 한글 폰트 로딩 상태와 12px 텍스트의 실제 가독성 확인

## 7. 신규 공통 홍보용 단일 페이지 변경 전 판단

- 이번 변경이 대응하는 사용자 요청: 기존 상세페이지를 덮어쓰지 않고 기업·일반 사용자가 함께 볼 수 있는 VR 공통 홍보용 단일 페이지 시안을 별도 경로에 만든다.
- 보존해야 하는 기존 동작: 현재 홈페이지, IMMERSA 페이지, 기존 `chemical-safety-training` 상세페이지, 문의 폼, Unity/XR 클라이언트 및 서버 API는 변경하지 않는다.

1. 기존 Inspector/씬 작성값을 보존하는가?
   - Unity 씬과 Inspector는 작업 범위 밖이며 변경하지 않는다. 기존 웹페이지의 작성값도 수정하지 않고 새 정적 페이지 하나만 추가한다.
2. 단일 기준 오브젝트와 상태 소유자는 무엇인가?
   - 새 페이지의 콘텐츠·레이아웃 기준은 신규 경로의 `index.html` 한 파일이다. 별도 런타임 상태는 두지 않는다.
3. 입력 이벤트, Interactor/Caster, Raycaster, Layer, Collider의 전체 경로는 무엇인가?
   - XR 입력은 사용하지 않는다. 브라우저 입력은 앵커 링크와 네이티브 `details/summary`만 사용하며, 기업 문의 CTA는 기존 홈페이지의 `#contact`로 이동한다.
4. 실패 시 런타임 자동 수리 대신 명확한 오류로 멈춰야 하는가?
   - 런타임 자동 생성·자동 수리·fallback을 추가하지 않는다. 에셋과 링크 누락은 정적 검증에서 오류로 확인한다.
5. UI·텔레포트·PPE Grab·거울·XR 양안 중 어떤 소비자가 함께 영향을 받는가?
   - 웹 데스크톱·태블릿·모바일 레이아웃과 기업·일반 사용자 메시지만 영향을 받는다. Unity UI, 텔레포트, PPE Grab, 거울, XR 양안에는 영향이 없다.
6. 변경 전 기준 실행과 변경 후 비교 실행은 무엇인가?
   - 변경 전 기준은 현재 `immersa/chemical-safety-training` 페이지다. 변경 후에는 신규 페이지의 HTML 구조, 내부 링크, 이미지 경로, 반응형 규칙을 비교하고 기존 페이지가 수정되지 않았는지 확인한다.
7. 정적 확인, Unity Editor 확인, Quest/OpenXR 확인 중 어디까지 실제로 검증했는가?
   - 변경 전에는 기존 HTML/CSS와 이미지 에셋을 정적으로 확인했다. 이번 웹 전용 작업에는 Unity Editor와 Quest/OpenXR 검증을 수행하지 않는다.

## 8. 신규 공통 홍보용 단일 페이지 시안

### 적용한 변경

- 기존 상세페이지를 보존하고 `server/public/site/immersa/chemical-safety-training-promo/index.html`을 새로 추가했다.
- 기업·일반 사용자 공통 히어로에 현재 콘텐츠 범위, 플랫폼, 언어, 출시 상태를 요약했다.
- 기업·기관과 일반 사용자를 분리한 두 개의 대상별 가치 카드를 구성했다.
- 기존 `ppe_room`, `check_work`, `select_ppe`, `ppe_check`, `check_ppe` 인게임 에셋을 활용해 작업 확인부터 착용 확인까지의 네 단계 흐름을 구성했다.
- 현재 확인된 제품 정보와 PPE 착용 교육 한정 범위를 명시했다.
- 기업용 도입 문의와 일반 사용자용 출시 상태 확인 CTA를 분리했다.
- 자바스크립트 없이 동작하는 FAQ, 키보드 포커스 표시, 본문 바로가기, 이미지 대체 텍스트, 모바일 단일 열 레이아웃을 적용했다.
- 검색 결과와 링크 공유를 위한 페이지 제목, 설명, Open Graph 제목·설명을 추가했다.

### 근본 원인

- 기존 상세페이지는 실제 체험 단계 소개에는 적합했지만, 기업과 일반 사용자가 각자의 관점에서 가치, 제품 상태, 다음 행동을 판단할 정보가 부족했다.
- 상단 Meta Horizon 안내가 실제 링크로 연결되지 않았고 제품 기본정보 영역은 공통 CSS에서 숨겨져 있었다.

### 영향 범위

- 신규 정적 HTML 한 파일만 추가했다.
- 기존 브랜드 홈페이지, IMMERSA 페이지, 기존 VR 상세페이지와 공통 CSS는 수정하지 않았다.
- Unity 클라이언트, XR 입력, 씬, 서버 API 및 문의 폼 동작에는 영향이 없다.

### 완료한 검증

- 신규 페이지에서 참조하는 이미지, 파비콘, 내부 페이지 링크가 모두 실제 파일에 연결되는지 확인했다.
- 페이지 내 ID 9개와 앵커 6개를 대조해 중복 ID, 누락된 대상, 빈 `href`가 없음을 확인했다.
- 이미지 8개에 모두 `alt`가 있고, FAQ 5개와 데스크톱·모바일 반응형 규칙이 포함됐는지 확인했다.
- 신규 페이지와 본 회의록에 대해 `git diff --check`를 실행해 공백 오류가 없음을 확인했다.
- 서버 전체 테스트는 `server/node_modules`가 없어 `express`를 불러오지 못했고, 기존 홈 캐러셀 테스트도 현재 배너 개수 차이로 실패했다. 신규 페이지의 정적 검증 결과와는 별도인 환경·기존 테스트 상태로 기록한다.

### 아직 필요한 수동 검증

- 실제 브라우저에서 데스크톱·태블릿·모바일 너비의 히어로, 로고, 두 CTA, 카드 줄바꿈을 확인한다.
- 키보드로 본문 바로가기, 페이지 내 이동, FAQ 열기·닫기 순서와 포커스 표시를 확인한다.
- 한글 시스템 폰트별 제목 줄바꿈과 기업·일반 사용자 문구의 가독성을 확인한다.
- 실제 배포 전 최종 지원 기기, Meta Horizon URL, 공개 일정이 확정되면 제품 정보와 CTA를 갱신한다.

## 9. 인게임 이미지 가로형 재구성 변경 전 판단

- 이번 변경이 대응하는 사용자 요청: 세로로 길게 쌓인 체험 구성을 줄이고 기존 16:9 인게임 이미지를 온전히 활용할 수 있는 가로형 페이지 리듬으로 수정한다.
- 보존해야 하는 기존 동작: 기존 페이지와 신규 시안의 기업·일반 사용자 메시지, 현재 PPE 교육 범위, 문의 링크, FAQ와 제품 정보는 유지한다.

1. 기존 Inspector/씬 작성값을 보존하는가?
   - Unity 씬과 Inspector, 기존 웹페이지는 변경하지 않는다. 신규 프로모션 시안의 작성값만 수정한다.
2. 단일 기준 오브젝트와 상태 소유자는 무엇인가?
   - 신규 프로모션 `index.html`이 계속 단일 기준이다. 체험 이미지 영역은 CSS 가로 스크롤 레일 하나가 소유한다.
3. 입력 이벤트, Interactor/Caster, Raycaster, Layer, Collider의 전체 경로는 무엇인가?
   - XR 입력은 없다. 가로 레일은 브라우저 기본 스크롤·터치 스와이프와 단계 앵커 링크만 사용한다.
4. 실패 시 런타임 자동 수리 대신 명확한 오류로 멈춰야 하는가?
   - 자바스크립트 자동 보정이나 fallback을 추가하지 않는다. 이미지 비율과 링크 대상은 정적 검증한다.
5. UI·텔레포트·PPE Grab·거울·XR 양안 중 어떤 소비자가 함께 영향을 받는가?
   - 신규 웹 시안의 체험 이미지 레이아웃과 데스크톱·모바일 스크롤만 영향을 받는다.
6. 변경 전 기준 실행과 변경 후 비교 실행은 무엇인가?
   - 변경 전 세로형 큰 이미지 1개와 2열 카드 4개 구성을 기준으로, 변경 후 5개 이미지를 같은 16:9 가로 레일에서 전체 노출하는지 비교한다.
7. 정적 확인, Unity Editor 확인, Quest/OpenXR 확인 중 어디까지 실제로 검증했는가?
   - HTML/CSS 정적 확인만 수행한다. Unity Editor와 Quest/OpenXR는 작업 범위 밖이다.

### 적용 결과

- 기존 체험 단계 아래와 문의 CTA 사이에 `PROJECT INFORMATION` 섹션을 추가했다.
- 프로젝트 정보는 콘텐츠 범위, 플랫폼, 조작 방식, 언어, 배포 상태를 가로 구분선 목록으로 표시한다.
- 프로젝트 정보 다음에 별도 상·하단 여백과 구분선을 가진 `FAQ` 섹션을 추가했다.
- FAQ는 출시 시점, 지원 기기, 현재 교육 범위, 기업용 데모·상담의 네 항목으로 구성했다.
- 기존 히어로, 체험 단계, 문의 CTA, 돌아가기 링크, 헤더와 푸터는 수정하지 않았다.

### 완료한 검증

- 프로젝트 정보 섹션 1개와 FAQ 섹션 1개가 `체험 단계 → 프로젝트 정보 → FAQ → 문의 CTA` 순서로 배치된 것을 확인했다.
- FAQ 항목 4개, 중복 ID 없음, 신규 빈 링크 없음과 `git diff --check` 통과를 확인했다.
- 페이지의 기존 Meta Horizon 준비 링크에는 이전부터 `href="#"`가 남아 있으며 이번 요청 범위에서는 변경하지 않았다.

### 아직 필요한 수동 검증

- 데스크톱과 모바일에서 프로젝트 정보 행 간격, FAQ 질문 줄바꿈, FAQ 열림 상태의 세로 여백을 확인한다.

## 12. 프로젝트 정보 상단 구분선

- 이번 변경은 기존 히어로 하단과 동일한 `1px solid rgba(125, 139, 153, 0.22)` 구분선을 프로젝트 정보 위에 추가하는 요청에만 대응한다.
- 기존 체험 흐름과 프로젝트 정보 사이의 전체 세로 간격은 유지하고, 기존 하단 여백을 선 위·아래로 나눠 배치한다.
- 프로젝트 정보, FAQ, CTA의 콘텐츠와 동작 및 Unity/XR 영역은 변경하지 않는다.

## 13. 상세페이지 상단 타이틀 이미지 중심 재배치

- 이번 변경이 대응하는 사용자 요청: 기존 시각적 텍스트 타이틀을 제거하고 `뱃지 → 가로 전체 타이틀 이미지 → 설명 → Meta Horizon 안내 → 기존 체험 카드` 순서로 상단을 재배치한다.
- 보존해야 하는 기존 동작: 기존 이미지 원본 비율, IMMERSA 톤앤매너, 체험 카드 이후의 프로젝트 정보·FAQ·문의 CTA·푸터는 변경하지 않는다.
- 검색과 접근성을 위해 문서의 `h1` 텍스트는 시각적으로만 숨기고 DOM에는 유지한다.
- 타이틀 이미지는 강제 crop 없이 `width:100%`, `height:auto`로 표시한다.
- XR 입력, Unity 씬, 서버 API에는 영향이 없으며 HTML/CSS 정적 검증만 수행한다.

### 적용 결과

- 기존 좌측 대형 텍스트 타이틀과 설명, 우측 대표 이미지의 2열 히어로 구성을 제거했다.
- 상단을 `프로젝트 라벨·기술 뱃지 → 전체 폭 대표 타이틀 이미지 → 콘텐츠 설명 → Meta Horizon 안내` 순서로 재배치했다.
- 대표 이미지 다음의 설명과 Meta Horizon 안내를 서로 다른 행으로 분리했다.
- 기존 PPE Room과 체험 카드, 프로젝트 정보, FAQ, 문의 CTA는 변경하지 않았다.

### 완료한 검증

- DOM 순서가 `뱃지 → 이미지 → 설명 → Meta Horizon 안내 → 체험 카드`와 일치하는 것을 확인했다.
- 문서에 `h1`이 한 개 존재하고 시각적으로 숨겨졌으며, 중복 ID와 대체 텍스트가 없는 이미지가 없음을 확인했다.
- 대표 이미지 선택자에 강제 `aspect-ratio`가 없고 `height:auto`로 전체 폭을 사용하는 것을 확인했다.
- `git diff --check`를 통과했다.

### 아직 필요한 수동 검증

- 실제 브라우저에서 전체 폭 대표 이미지의 크기, 뱃지 줄바꿈, 설명과 Meta Horizon 안내 사이 간격을 확인한다.

## 14. 상단 설명·Meta 안내 사이 구분선 제거

- `PROJECT 01` 아래의 기술 뱃지 배치는 유지했다.
- `ABOUT THE EXPERIENCE` 설명과 Meta Horizon 안내 사이에 추가했던 회색 상단 테두리만 제거했다.
- 나머지 상단 순서와 간격, 하단 콘텐츠는 변경하지 않았다.

## 15. 기술 뱃지와 ABOUT 설명 위치 조정

- 데스크톱에서도 기술 뱃지가 `TYCHE IMMERSA / PROJECT 01` 아래 줄에 오도록 상단 뱃지 영역을 세로 방향으로 고정했다.
- 기술 뱃지는 왼쪽 정렬을 사용한다.
- `ABOUT THE EXPERIENCE` 설명을 Meta Horizon 안내 카드 아래로 이동했다.
- 대표 이미지, Meta Horizon 카드, 기존 체험 카드와 하단 섹션의 내용은 변경하지 않았다.

## 16. 기술 뱃지 오른쪽 정렬

- `PROJECT 01`은 왼쪽 정렬을 유지했다.
- 아래 줄의 기술 뱃지 컨테이너를 전체 폭으로 확장하고 뱃지만 오른쪽 정렬했다.

## 17. ABOUT 라벨을 기술 뱃지 윗줄로 이동

- `PROJECT 01` 아래에 한 줄 분량의 추가 여백을 두었다.
- `ABOUT THE EXPERIENCE` 라벨을 오른쪽 정렬 기술 뱃지의 바로 윗줄로 이동했다.
- Meta Horizon 카드 아래에는 기존 설명 문단만 유지했다.

## 18. ABOUT 라벨과 설명을 한 묶음으로 이동

- `ABOUT THE EXPERIENCE` 라벨 아래에 기존 설명 문단을 함께 배치했다.
- 라벨과 설명 묶음 다음에 오른쪽 정렬 기술 뱃지가 이어진다.
- Meta Horizon 카드 아래에 분리되어 있던 설명 영역은 제거했다.

## 19. ABOUT 영역을 Meta 카드 하단 두 구분선 사이로 이동

- 상단 뱃지 영역에서 `ABOUT THE EXPERIENCE` 라벨과 설명을 제거했다.
- Meta Horizon 카드 아래의 기존 히어로 하단 회색 선 다음에 ABOUT 라벨과 설명을 독립 섹션으로 배치했다.
- ABOUT 섹션 아래에 동일한 얇고 흐린 회색 구분선을 하나 더 추가했다.
- 두 구분선 다음에는 기존 PPE Room과 체험 카드가 이어진다.

## 20. PROJECT·ABOUT 영역 여백 확대

- `PROJECT 01`과 기술 뱃지 사이 여백을 기존보다 한 줄 분량 늘렸다.
- ABOUT 독립 섹션의 위·아래 여백을 데스크톱 `62px`, 모바일 `50px`로 확대했다.
- 구분선과 콘텐츠 순서는 변경하지 않았다.

## 21. 타이틀 이미지 한글 텍스트 색면 정리

### 적용한 변경

- 기존 `metahorizon_title_2560x1440.png`는 보존했다.
- 이미지 편집 도구를 사용해 `화학물질 안전훈련` 글자 내부의 얼룩·체커 형태 질감만 매끈한 블루–그린 그라데이션으로 정리한 `metahorizon_title_clean.png`를 추가했다.
- 기존 상세페이지의 대표 이미지 참조만 새 정리본으로 교체하고 실제 크기 `1672×941`을 HTML에 기록했다.

### 영향 범위

- 화학물질 안전훈련 VR 상세페이지의 상단 대표 이미지 한 장만 영향을 받는다.
- 원본 이미지, 다른 배너, Unity/XR 클라이언트와 서버 API는 변경하지 않았다.

### 완료한 검증

- 정리본의 한글 문구, 16:9 가로 구성, 블루–그린 색상 흐름과 주요 이미지 요소를 확인했다.
- 새 이미지 파일이 프로젝트 에셋 경로에 존재하고 상세페이지 참조가 연결된 것을 확인했다.

### 아직 필요한 수동 검증

- 실제 브라우저의 전체 폭 표시에서 한글 글자 내부 색면과 가장자리 선명도를 확인한다.

## 22. 타이틀 이미지 한글 색면 2차 정리

- 1차 정리본에 남은 미세한 얼룩, 사각 패턴, 색 농도 편차를 더 강하게 제거했다.
- 한글 문구와 글자 형태, 블루–그린 방향, 나머지 장면을 유지한 `metahorizon_title_clean_v2.png`를 추가했다.
- 기존 원본과 1차 정리본은 보존하고 상세페이지 참조만 2차 정리본으로 교체했다.
- 결과 크기는 `1672×941`이다.

## 23. 프로젝트 정보·FAQ 폭과 정렬 조정

- 프로젝트 정보 제목에서 `현재 확인된`을 제거했다.
- 프로젝트 정보 목록과 FAQ 목록의 최대 폭을 `860px`로 제한해 가로선 길이를 줄였다.
- 두 섹션의 제목, 설명, 프로젝트 정보 항목, FAQ 질문과 답변을 가운데 정렬했다.
- FAQ 펼침 아이콘은 질문 중앙 정렬에 영향을 주지 않도록 오른쪽에 별도로 배치했다.

## 24. 프로젝트 정보·FAQ 텍스트 왼쪽 정렬

- 프로젝트 정보와 FAQ의 `860px` 콘텐츠 영역은 페이지 가운데 정렬을 유지했다.
- 영역 내부의 제목, 설명, 프로젝트 정보 항목, FAQ 질문과 답변은 모두 왼쪽 정렬로 수정했다.
- FAQ 펼침 아이콘은 오른쪽 위치를 유지했다.

## 25. 프로젝트 정보·FAQ 차콜 텍스트 적용

- 프로젝트 정보와 FAQ 섹션 전용 텍스트 색상 `#30343B`를 추가했다.
- 두 섹션의 영문 라벨, 제목, 설명, 프로젝트 정보 항목명·값, FAQ 질문·답변과 펼침 아이콘에 동일한 진한 차콜 색상을 적용했다.
- 구분선 색상과 다른 페이지 영역의 색상은 변경하지 않았다.

## 26. 프로젝트 정보·FAQ 보조 텍스트 색상 범위 수정

- 전체 텍스트를 차콜로 통일한 변경을 되돌렸다.
- 섹션 제목과 주요 값·질문은 기존 `var(--ink)`, 영문 라벨과 FAQ 펼침 아이콘은 기존 `var(--blue)`를 사용한다.
- 원래 연한 회색이던 섹션 설명, 프로젝트 정보 항목명, FAQ 답변만 기존보다 살짝 진한 회색 `#5B6169`로 변경했다.
- 이를 통해 제목·주요 정보·보조 설명의 대비 단계가 다시 구분되도록 했다.

## 27. 2026-08-26 최종 작업 요약

### 적용한 변경

- 기존 화학물질 안전훈련 VR 상세페이지의 상단을 텍스트 대형 타이틀 중심 2열 구성에서 대표 타이틀 이미지 중심 구성으로 변경했다.
- 상단 순서는 `PROJECT 01 → 오른쪽 정렬 기술 뱃지 → 전체 폭 타이틀 이미지 → Meta Horizon 안내 카드 → 회색 구분선 → ABOUT THE EXPERIENCE 설명 → 회색 구분선 → 기존 체험 카드`로 정리했다.
- 시각적 대형 제목은 제거했지만 검색과 접근성을 위한 `h1`은 화면 밖에 유지했다.
- `PROJECT 01` 아래 여백과 ABOUT 영역의 위·아래 여백을 확대했다.
- 기존 체험 흐름 아래에 `프로젝트 정보`와 `FAQ`를 별도 상하 섹션으로 추가했다.
- 프로젝트 정보 위에는 기존 히어로 하단과 동일한 얇고 흐린 회색 구분선을 추가했다.
- 프로젝트 정보와 FAQ의 콘텐츠 영역을 최대 `860px`로 제한해 페이지 가운데 배치하고, 내부 텍스트는 왼쪽 정렬했다.
- 프로젝트 정보 제목은 `프로젝트 정보`로 단순화했다.
- 기존에 연한 회색이던 섹션 설명, 프로젝트 정보 항목명, FAQ 답변만 `#5B6169`로 조정하고 제목·주요 값·질문·파란색 라벨과 아이콘의 대비 체계는 유지했다.
- 타이틀 이미지의 `화학물질 안전훈련` 글자 내부 얼룩·체커 질감을 두 차례 정리했다.
  - 원본: `server/public/site/assets/metahorizon_title_2560x1440.png`
  - 1차 정리본: `server/public/site/assets/metahorizon_title_clean.png`
  - 최종 적용본: `server/public/site/assets/metahorizon_title_clean_v2.png`
- 원본과 1차 정리본은 보존하고 기존 상세페이지는 최종 2차 정리본을 사용한다.
- 기업·일반 사용자 공통 홍보 구성을 검토하기 위한 별도 초안 `server/public/site/immersa/chemical-safety-training-promo/index.html`을 만들었으나 기존 공개 경로와 내비게이션에는 연결하지 않았다.

### 판단 오류와 수정 방향

- 모든 인게임 이미지가 가로형임을 확인했는데도 초기 별도 초안에 세로형 카드 패턴을 우선 적용해 원본 이미지의 장점을 살리지 못했다.
- 기존 IMMERSA 톤앤매너를 유지해야 하는 작업에서 별도 다크 네이비·라임 콘셉트를 적용했다가 제거했다.
- 사용자 요청이 기존 회색 보조 텍스트의 농도 조정이었는데 제목·값·아이콘까지 차콜로 통일해 정보 위계를 없앴다가, 보조 텍스트 범위에만 `#5B6169`를 적용하도록 수정했다.
- 이후 변경은 기존 `immersa.css`, 현재 상세페이지, 실제 이미지 비율을 기준으로 제한했다.

### 영향 범위

- 수정한 공개 페이지: `server/public/site/immersa/chemical-safety-training/index.html`
- 추가한 이미지: `metahorizon_title_clean.png`, `metahorizon_title_clean_v2.png`
- 추가한 미연결 초안: `server/public/site/immersa/chemical-safety-training-promo/index.html`
- Unity 클라이언트, XR 입력, 씬, 서버 API, 문의 폼 동작은 변경하지 않았다.

### 완료한 검증

- 상단 DOM 순서, 접근성용 단일 `h1`, 프로젝트 정보·FAQ 순서와 중복 ID 여부를 정적으로 확인했다.
- 이미지와 내부 링크 참조, 이미지 대체 텍스트, 프로젝트 정보·FAQ 최대 폭과 텍스트 정렬을 확인했다.
- 최종 타이틀 이미지가 `1672×941` 크기로 프로젝트 에셋 경로에 존재하고 기존 상세페이지에서 참조되는 것을 확인했다.
- 작업 대상 HTML에 대해 `git diff --check`를 반복 실행해 공백 오류가 없음을 확인했다.
- 서버 전체 테스트는 `server/node_modules`가 없어 `express`를 불러오지 못했고, 기존 홈 캐러셀 테스트도 현재 배너 개수 차이로 실패했다. 이번 정적 상세페이지 변경과는 별도인 환경·기존 테스트 상태다.

### 아직 필요한 수동 검증

- 실제 브라우저에서 데스크톱·태블릿·모바일 너비의 상단 뱃지 줄바꿈, 전체 폭 이미지, Meta Horizon 카드, ABOUT 영역 간격을 확인한다.
- 프로젝트 정보와 FAQ의 `860px` 영역 중앙 배치, 내부 왼쪽 정렬, 모바일 한 열 표시와 FAQ 열림 상태를 확인한다.
- 전체 폭 타이틀 이미지에서 한글 글자 내부 색면의 얼룩 제거 수준과 가장자리 선명도를 최종 확인한다.
- Meta Horizon 실제 URL과 최종 지원 기기·배포 일정이 확정되면 현재 준비 중 안내와 `href="#"` 링크를 갱신한다.

### 적용 결과

- 기존의 큰 세로 섹션 1개와 2열 카드 4개 구성을 제거하고, PPE Room을 포함한 인게임 이미지 5개를 하나의 가로 스크롤 레일로 통합했다.
- 모든 이미지는 원본 16:9 비율과 `object-fit: contain`을 사용해 잘리지 않고 전체가 보이도록 했다.
- 데스크톱에서는 하단 가로 스크롤과 `00`~`04` 장면 바로가기를 제공하고, 모바일에서는 한 번에 다음 장면이 일부 보이는 92% 폭의 스와이프 카드로 구성했다.
- 자바스크립트 없이 브라우저 기본 스크롤, 터치 스와이프, 키보드 포커스와 앵커 이동만 사용했다.

### 완료한 검증

- 가로 레일에 인게임 카드 5개가 존재하고 이전 `step-grid`가 남아 있지 않은 것을 확인했다.
- 파일 참조 18개가 모두 존재하며 중복 ID, 누락된 앵커 대상, 빈 링크가 없음을 확인했다.
- 신규 HTML에 대해 `git diff --no-index --check`를 실행해 공백 오류가 없음을 확인했다.

### 아직 필요한 수동 검증

- 실제 데스크톱 브라우저에서 마우스·트랙패드 가로 이동과 `00`~`04` 앵커 이동 위치를 확인한다.
- 모바일 브라우저에서 카드 스와이프, 스크롤 스냅, 이미지 전체 노출과 본문 세로 스크롤 간 간섭 여부를 확인한다.

## 10. 가로형 원본 기준 전면 재구성 변경 전 판단

- 이번 변경이 대응하는 사용자 요청: 모든 인게임 스크린샷이 가로형이라는 기본 조건을 레이아웃의 출발점으로 삼고 세로형 카드 중심 시안을 전면 교체한다.
- 보존해야 하는 기존 동작: 기존 공개 상세페이지, 문의 링크, 사실로 확인된 PPE 교육 범위와 출시 상태는 유지한다.

1. 기존 Inspector/씬 작성값을 보존하는가?
   - Unity 씬과 기존 공개 웹페이지는 변경하지 않는다. 신규 프로모션 시안만 전면 재작성한다.
2. 단일 기준 오브젝트와 상태 소유자는 무엇인가?
   - 각 이미지의 실제 원본 가로세로 비율을 레이아웃 기준으로 삼고 신규 `index.html` 한 파일이 페이지 구성을 소유한다.
3. 입력 이벤트, Interactor/Caster, Raycaster, Layer, Collider의 전체 경로는 무엇인가?
   - XR 입력은 없다. 일반 앵커 이동과 네이티브 FAQ만 사용하며 이미지 갤러리용 별도 입력 소비자를 만들지 않는다.
4. 실패 시 런타임 자동 수리 대신 명확한 오류로 멈춰야 하는가?
   - CSS에서 임의 높이·세로 비율·강제 crop으로 보정하지 않는다. 원본 `width`와 `height`를 사용하고 정적 검증에서 강제 비율과 `object-fit: cover`가 없는지 확인한다.
5. UI·텔레포트·PPE Grab·거울·XR 양안 중 어떤 소비자가 함께 영향을 받는가?
   - 신규 웹 시안의 데스크톱·모바일 레이아웃만 영향을 받는다.
6. 변경 전 기준 실행과 변경 후 비교 실행은 무엇인가?
   - 변경 전 세로형 audience 카드와 스크롤 카드 레일을 제거하고, 변경 후 모든 인게임 이미지를 원본 비율의 큰 가로 패널과 옆 설명으로 배치했는지 비교한다.
7. 정적 확인, Unity Editor 확인, Quest/OpenXR 확인 중 어디까지 실제로 검증했는가?
   - 이미지 원본 크기와 HTML/CSS를 정적으로 검증한다. Unity Editor와 Quest/OpenXR는 작업 범위 밖이다.

### 기존 톤앤매너 보존 기준 추가

- 신규 시안의 색상, 타이포그래피, 헤더, 푸터, 버튼, 테두리, 라운드와 여백 기준은 기존 `immersa/immersa.css`와 현재 `chemical-safety-training` 상세페이지를 그대로 따른다.
- 별도의 다크 네이비·라임 콘셉트, 격자 배경, 신규 브랜드 컬러와 과도한 장식은 사용하지 않는다.
- 변경 대상은 콘텐츠 정보 구조와 가로형 이미지 배치뿐이며 기존 IMMERSA의 흰 배경·블루 포인트·연한 회색 선·절제된 그림자 톤은 보존한다.

### 적용 결과

- 다크 네이비·라임 중심의 신규 콘셉트를 제거하고 기존 `immersa.css`를 직접 불러와 헤더, 내비게이션, 푸터, 브랜드 변수와 기본 타이포그래피를 재사용했다.
- 흰 배경, 파란색 포인트, 연한 회색 테두리, 18~28px 라운드와 기존 상세페이지 수준의 절제된 그림자만 사용했다.
- 세로형 audience 카드와 가로 스크롤 카드 레일을 제거했다.
- 히어로 대표 이미지는 문구 아래 전체 폭으로 배치하고, 인게임 화면 5개는 큰 가로 이미지와 짧은 옆 설명이 결합된 수평 행으로 구성했다.
- 모든 인게임 이미지는 작성된 `width`와 `height` 및 `height:auto`만 사용한다. 강제 `aspect-ratio`와 `object-fit:cover`는 사용하지 않는다.

### 완료한 검증

- 대표 이미지와 인게임 스크린샷 5개가 모두 새 페이지에서 참조되는 것을 확인했다.
- 강제 `aspect-ratio`, `object-fit:cover`, 이전 다크 네이비·라임 색상값이 남아 있지 않은 것을 확인했다.
- 파일 참조 26개, ID 7개, 앵커 3개를 대조해 누락 파일, 중복 ID, 누락 앵커가 없음을 확인했다.
- 이미지 8개에 모두 대체 텍스트가 있는지 확인했다.
- 신규 HTML과 회의록에 대해 `git diff --no-index --check`를 실행해 공백 오류가 없음을 확인했다.

### 아직 필요한 수동 검증

- 기존 IMMERSA 상세페이지와 나란히 열어 헤더·푸터·블루 포인트·텍스트 크기·라운드가 같은 브랜드 계열로 보이는지 확인한다.
- 데스크톱과 모바일에서 가로 이미지가 잘리지 않고 옆 설명과 자연스럽게 연결되는지 확인한다.

## 11. 기존 상세페이지 하단 프로젝트 정보·FAQ 추가

- 이번 변경이 대응하는 사용자 요청: 별도 프로모션 시안의 `프로젝트 정보`와 `FAQ`만 기존 화학물질 안전훈련 VR 상세페이지의 체험 흐름 아래에 추가하고, 두 콘텐츠를 위아래 독립 섹션으로 구분한다.
- 보존해야 하는 기존 동작: 기존 히어로, 이미지, 체험 단계, 문의 CTA, 돌아가기 링크, 헤더·푸터와 별도 프로모션 시안은 변경하지 않는다.

1. 기존 Inspector/씬 작성값을 보존하는가?
   - Unity 영역은 변경하지 않고 기존 상세페이지의 작성된 HTML/CSS도 그대로 보존하며 새 섹션과 해당 선택자만 추가한다.
2. 단일 기준 오브젝트와 상태 소유자는 무엇인가?
   - 기존 `chemical-safety-training/index.html`이 두 신규 섹션의 콘텐츠와 레이아웃을 소유한다.
3. 입력 이벤트, Interactor/Caster, Raycaster, Layer, Collider의 전체 경로는 무엇인가?
   - 프로젝트 정보는 정적 텍스트이며 FAQ는 브라우저 네이티브 `details/summary`만 사용한다.
4. 실패 시 런타임 자동 수리 대신 명확한 오류로 멈춰야 하는가?
   - 자바스크립트, 자동 생성, fallback을 추가하지 않는다. 구조와 ID를 정적으로 검증한다.
5. UI·텔레포트·PPE Grab·거울·XR 양안 중 어떤 소비자가 함께 영향을 받는가?
   - 기존 웹 상세페이지의 하단 데스크톱·모바일 레이아웃만 영향을 받는다.
6. 변경 전 기준 실행과 변경 후 비교 실행은 무엇인가?
   - 기존 체험 단계 다음에 바로 문의 CTA가 나오는 구조를 기준으로, 그 사이에 프로젝트 정보 섹션과 FAQ 섹션만 순서대로 추가됐는지 비교한다.
7. 정적 확인, Unity Editor 확인, Quest/OpenXR 확인 중 어디까지 실제로 검증했는가?
   - HTML/CSS 정적 확인만 수행한다. Unity Editor와 Quest/OpenXR는 작업 범위 밖이다.

## 28. 2026-08-28 홈 1번 VR 배너 오른쪽 배경색 정리

### 적용한 변경

- `Now on Tyche` 첫 번째 화학물질 안전훈련 VR 배너에서 원본 가로 이미지가 카드 폭보다 짧을 때
  오른쪽에 노출되던 연한 파란색 `#eaf5ff` 배경을 흰색 `#fff`로 변경했다.
- `Now on Tyche` 캐러셀 카드 전체에 `1px solid rgba(32,32,32,.06)` 외곽선을 추가해
  흰 배경과의 경계만 매우 연하게 구분되도록 조정했다.
- 이미지의 `object-fit: contain`, 원본 비율, 왼쪽 정보 오버레이, 캐러셀 이동 동작은 유지했다.

### 근본 원인

- 첫 번째 배너의 이미지 전체 노출을 위해 `contain`을 사용하면서 남는 영역이 생기고,
  `.release-media-banner`와 `.release-slide-vr`의 파란 배경색이 그 영역을 통해 보였다.

### 영향 범위

- 홈페이지 `Now on Tyche` 첫 번째 VR 배너의 빈 배경 영역과 캐러셀 카드 외곽선만 영향을 받는다.
- 다른 두 배너의 콘텐츠·배경, 하위 페이지, 문의 폼, 서버 API와 Unity/XR 클라이언트는 변경하지 않았다.

### 완료한 검증

- 두 배너 배경 선택자가 모두 `#fff`를 사용하는지 검사하는 정적 회귀 테스트를 추가했다.
- 최종 `.release-carousel` 규칙이 매우 연한 외곽선 변수 `--line-soft`를 사용하는지 검사하는
  정적 회귀 테스트를 추가했다.
- `npm test` 결과 전체 19개 테스트가 통과했다.
- CSS와 테스트 파일에 대해 `git diff --check`를 통과했다.

### 아직 필요한 수동 검증

- 운영 반영 후 데스크톱·모바일에서 배너 오른쪽 남는 영역이 흰색으로 보이고 이미지 경계가
  어색하지 않은지 실제 브라우저에서 확인한다.
- 데스크톱·모바일에서 외곽선이 콘텐츠보다 두드러지지 않고 흰 배경과의 경계만 구분하는지 확인한다.


## 29. 2026-08-28 홈 포인트 컬러와 세부 라인 컬러 분리

### 피드백과 결정

홈페이지의 기존 오렌지 포인트가 SPARK의 라인 컬러와 겹쳐 TYCHE WORKS 모기업 홈과 세부 라인의 아이덴티티 구분이 약하다는 피드백을 반영했다. 세부 라인은 IMMERSA 블루 `#2878FF`, SPARK 오렌지 `#FF7A00`, LOOP 옐로 `#F4C430`을 유지하고, 모기업 홈과 공통 브랜드 영역의 포인트 컬러는 세 색과 겹치지 않는 바이올렛 `#6C4BD8`로 구분한다.

### 적용한 변경

- 공통 홈 변수 `--orange`를 의미가 분명한 `--brand-accent`와 `--brand-accent-rgb`로 교체했다.
- 홈의 영문 라벨, 주요 버튼, 보조 버튼 hover, 히어로 그래픽, 브랜드 소개 링크와 문의 입력 focus·버튼 hover에 바이올렛을 적용했다.
- 홈의 현재 공개 라인인 IMMERSA 카드가 오렌지로 강조되던 부분은 IMMERSA 블루로 수정했다.
- 캐러셀 내부의 VR·SPARK·LOOP 고유 색상, 로고, 이미지, 콘텐츠와 전환 동작은 변경하지 않았다.

### 영향 범위와 검증

- 구현 변경 파일은 홈페이지 공통 스타일과 정적 회귀 테스트다. 서버 API, 텔레메트리, DB와 Unity 클라이언트 코드는 변경하지 않았다.
- 홈 브랜드 포인트와 세 라인의 색상값, IMMERSA 카드 강조색, 기존 오렌지 변수 미사용을 자동 테스트에서 검사한다.

### 운영 배포 성공

- 2026-08-28 서버 `main`의 `8961843821050e606619fd9116db1b0153f84108` 커밋을 Vultr 운영 서버에 fast-forward로 반영했다.
- `https://tycheworks.com/`, `/brand`, IMMERSA·SPARK·LOOP 공개 홈페이지가 모두 HTTP `200`을 반환했다.
- 공개 `styles.css`에서 홈 바이올렛 `#6C4BD8`과 IMMERSA 블루 `#2878FF`를 확인했고, 폐기한 `var(--orange)` 참조가 없음을 확인했다.
- 운영 서버에서 자동 테스트 20개 통과, `npm audit --audit-level=high` 취약점 0개, `git diff --check`와 `nginx -t` 통과를 확인했다.
- Nginx가 정적 파일을 직접 제공하므로 PM2를 재시작하지 않았다. 운영 DB, 마이그레이션, 텔레메트리와 Unity 클라이언트 코드는 변경하지 않았다.

### 남은 수동 검증

- 실제 데스크톱·모바일 브라우저에서 바이올렛의 밝기와 흰색 버튼 글자 대비, 히어로 그래픽의 농도를 육안 확인한다.

## 30. 외부 배포용 화학물질 안전훈련 VR 랜딩 패키지

### 적용한 변경

- 기존 브랜드 홈페이지와 VR 상세페이지를 수정하거나 내비게이션에 연결하지 않고 `public/site/chemical-safety-vr-landing/`을 독립 정적 패키지로 추가했다.
- 한 화면 게임 스플래시형 다크 버전과 동일 구조의 화이트 버전을 분리했다.
  - 다크: `/chemical-safety-vr-landing/`
  - 화이트: `/chemical-safety-vr-landing/light/`
- 장문 광고 캠페인형은 `/chemical-safety-vr-landing/campaign/`에 유지했다.
- 모든 주요 CTA는 기존 공개 VR 상세페이지 `https://immersa.tycheworks.com/chemical-safety-training`으로 연결한다.
- 저장소의 공식 `immersa-line-logo.png`를 재사용했으며 신규 로고를 만들지 않았다.
- 사용자가 제공한 `landing_vr.png` 1049×588 원본을 기준으로 구도, 한글 제목과 VR 심볼을 보존한 1672×941 고해상도 버전 `landing_vr_hd.png`을 제작해 스플래시 대표 이미지로 적용했다.
- 대표 이미지는 `object-fit: contain`으로 표시해 모자 위 여백과 이미지 하단까지 잘리지 않게 했다. 모바일에서도 이미지 전체 폭을 먼저 표시하도록 별도 규칙을 적용했다.
- 사용자 검토 결과에 따라 하단 구분선, IMMERSA 로고, 중앙 문구와 저작권 바는 다크·화이트 버전에서 제거했다.
- 정적 자산은 랜딩 폴더 안에 포함해 기존 사이트 자산 경로와 분리했다.

### 근본 원인

- 기존 상세페이지는 제품 설명 목적이어서 외부 광고 매체에 바로 배포할 한 화면 진입 페이지 역할과 달랐다.
- 초기 대표 이미지에는 우측 상단 로고가 포함돼 화면 비율에 따라 잘렸고, 별도 헤더 로고와 중복됐다.
- 사용자가 제공한 최종 원본은 1049×588이어서 약 1920px 화면에서 확대 시 선명도가 떨어졌다.
- 풀스크린 `cover`는 넓은 화면에서 상하를, 세로 화면에서 좌우를 잘라 보호복 모자와 이미지 타이틀 일부가 보이지 않는 문제가 있었다.

### 영향 범위

- 신규 정적 랜딩 폴더, 랜딩용 이미지와 본 회의록만 영향을 받는다.
- 기존 브랜드 홈페이지, IMMERSA 페이지, 화학물질 안전훈련 VR 상세페이지와 기존 내비게이션은 변경하지 않았다.
- Express API, MariaDB 스키마, 텔레메트리, Unity 클라이언트와 운영 배포에는 영향이 없다.

### 완료한 검증

- 다크·화이트·캠페인 랜딩과 참조 CSS·이미지가 로컬 Express에서 HTTP 200을 반환하는지 확인했다.
- 공개 VR 상세페이지가 HTTP 200을 반환하는지 확인했다.
- 다크·화이트 버전에서 제거 대상 하단 바 마크업과 스타일이 남아 있지 않은지 정적으로 확인했다.
- `git diff --check`를 통과했다.
- 서버 `npm test` 결과 34개 테스트가 통과하고 실패가 없었다.
- 사용자 제공 화면을 기준으로 로고 중복, 이미지 크롭과 하단 바를 순차적으로 조정했다.

### 아직 필요한 수동 검증

- 최종 다크·화이트 버전을 실제 데스크톱과 모바일 전체 화면에서 다시 열어 이미지 여백, 제목 대비와 CTA 위치를 육안 확인한다.
- 광고 매체에 연결할 최종 URL, UTM 파라미터와 공개 시점을 확정한다.
- 운영 배포는 이번 작업에 포함하지 않는다.

## 31. 화학물질 안전훈련 VR 개인정보처리방침 링크

### 적용한 변경

- 공개 VR 상세페이지 `public/site/immersa/chemical-safety-training/index.html`의 기존 `COMPANY`
  푸터 그룹에 `VR PRIVACY POLICY` 링크를 한 개 추가했다.
- 링크는 GitHub 저장소 화면이 아니라 실제 공개된 VR 전용 개인정보처리방침 페이지
  `https://softcastella.github.io/tycheworks-safetytrainingvr-privacy/`로 연결한다.
- 개인정보처리방침은 새 창으로 열고, 새 창에서 원래 상세페이지를 제어하지 못하도록
  `rel="noopener noreferrer"`를 함께 적용했다.
- 해당 방침은 화학물질 안전훈련 VR과 Meta·XR 데이터 처리를 대상으로 하므로 TYCHE WORKS 전체
  서비스의 공용 개인정보처리방침으로 표시하지 않는다.

### 영향 범위

- 화학물질 안전훈련 VR 상세페이지 푸터와 해당 링크를 확인하는 자동 테스트만 변경했다.
- 브랜드 홈, 다른 라인 페이지, 독립 랜딩 패키지, API, DB와 Unity 클라이언트에는 영향이 없다.
- 배포와 Meta 개발자 대시보드의 URL 변경은 이번 작업에 포함하지 않았다.

### 완료한 검증

- 공개 개인정보처리방침 페이지가 HTTP 200을 반환하는 것을 확인했다.
- VR 상세페이지에 공개 URL과 `VR PRIVACY POLICY` 문구가 각각 한 번만 존재하는지 확인했다.
- GitHub 저장소 화면 주소를 링크하지 않는 회귀 테스트를 추가했다.
- `git diff --check`는 오류 없이 통과했고, `npm test`는 35개 테스트가 모두 통과했다.

이 문서는 서버 미러에서 갱신했다. `Docs/SharedDocumentManifest.md`의 클라이언트 기준본에 같은 변경을
동기화해야 하며, 동기화 전에는 양쪽 공용 문서 전체가 일치한다고 보고하지 않는다.

## 32. VR 상세페이지 교육·훈련·테스트 모드 안내

### 적용한 변경

- 상단 `ABOUT THE EXPERIENCE`에 현재 콘텐츠가 화학물질 작업 전 PPE 착용 안전교육임을 먼저
  밝히는 제목과 설명을 추가했다. 작업계획 확인, PPE 선택, 오염·손상 판별, 순서에 맞는 착용과
  거울 확인까지 교육 범위를 한 문단에서 설명한다.
- 히어로의 범용 `Safety Training` 배지를 `PPE Training`으로 교체하고 검색·공유 설명과 구조화
  데이터도 PPE 착용 교육 범위에 맞췄다.
- 공개 VR 상세페이지의 상태 표시와 기존 체험 순서 사이에 `THREE WAYS TO LEARN` 섹션을 추가했다.
- 클라이언트 현재 빌드 씬 `Assets/Scenes/3_PPE_Room_3mode_loco.unity`와 모드 처리 코드를 기준으로
  교육·훈련·테스트 모드의 안내 수준, 오답 처리와 결과 표시 차이를 세 카드로 설명했다.
- 공통 흐름은 작업 시나리오 선택, PPE 선택·상태 판단·착용, 거울 확인과 5문항 퀴즈로 정리했다.
- 콘텐츠 정의는 특정 시나리오 목록에 고정하지 않고 `작업 시나리오 기반 PPE 착용 안전교육`으로
  표현했다. 밀폐공간과 누출 대응은 `현재 시나리오`로 분리해 향후 시나리오가 추가될 수 있는 정보
  구조를 유지했다.
- 프로젝트 정보에 `학습 구성` 행을 추가하고 페이지 검색 설명에 세 모드를 반영했다.
- 데스크톱에서는 세 카드를 한 행에, 900px 이하에서는 한 열에 표시하며 모바일 간격과 카드 크기를
  별도로 조정했다.
- 공유 모달의 플랫폼 이름과 `LINE` 워드마크를 보조 텍스트 최솟값인 11px로 올렸다. `링크 복사`
  아이콘은 상대 좌표가 어색하게 이어지던 체인 경로 대신, 복사 동작을 바로 알아볼 수 있는 겹친
  사각형 아이콘으로 교체했다.

### 근본 원인

- 기존 상세페이지는 PPE Room과 네 단계 체험 순서만 보여 주어, 실제 클라이언트에 구현된 세 모드의
  목적과 피드백 차이를 방문자가 알 수 없었다.
- 세 모드 설명보다 먼저 나와야 할 교육 대상과 범위가 한 줄의 포괄적인 안전교육 문구로만 표현되어,
  이 콘텐츠가 PPE 선택·판별·착용을 다룬다는 점이 불분명했다.
- 공유 모달에는 9px `LINE` 워드마크와 10px 플랫폼 이름이 남아 있어 보조 텍스트 11px 기준보다
  작았고, 링크 복사 SVG는 여러 상대 좌표 명령이 한 경로에서 이어져 체인 고리 형태가 불명확했다.

### 영향 범위

- 공개 VR 상세페이지 HTML/CSS, 정적 회귀 테스트와 본 문서만 변경한다.
- Unity 클라이언트 코드와 씬, Express API, DB, 텔레메트리, 배포 상태는 변경하지 않는다.

### 검증 기준

- 세 모드 카드 수, 모드별 핵심 문구, 공통 수행 흐름, 프로젝트 정보와 섹션 배치 순서를 자동 테스트로
  확인한다.
- 공유 옵션과 `LINE` 워드마크의 11px 적용 및 겹친 사각형 복사 아이콘을 자동 테스트로 고정한다.
- 서버 기본 회귀 테스트와 `git diff --check`를 실행한다.

### 아직 필요한 수동 검증

- 실제 데스크톱·태블릿·모바일 브라우저에서 카드 높이, 문장 줄바꿈과 색 대비를 육안 확인한다.
- Unity Play Mode와 Quest에서 세 모드 전체 수행을 다시 확인하는 작업은 이번 웹 변경 범위에 포함하지
  않는다. 웹 문구는 클라이언트 코드와 현재 빌드 씬의 정적 근거를 기준으로 작성했다.
- 테스트 작업계획 무작위 선택은 코드에 존재하지만 현재 빌드 씬의 `testRandomTestButton` 참조가 비어
  있어 상세페이지의 제공 기능으로 표시하지 않았다.

이 공용 문서는 서버 미러에서만 갱신했다. 클라이언트 기준본에는 31절과 32절 동기화가 필요하다.
